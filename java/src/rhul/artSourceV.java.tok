package ID.ID.ID.ID.ID.ID;

import ID.ID.ID;

import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.ID;
import ID.ID.ID.ID.ID.ID.*;


public class ID {
  private static void ID(ID ID, ID ID, ID ID) throws ID, ID {
    COMMENT_LINE_C
    if (ID.ID.ID() == INTEGER)
      throw new ID(STRING_DQ);
    else {
      ID ID = STRING_DQ;
      if (!ID.ID.ID()) ID = ID.ID.ID(INTEGER);
      ID.ID(ID.ID);
      ID.ID(ID.ID.ID(INTEGER), null);
      ID.ID(ID, false);
    }
  }

  public static void ID(final ID[] ID) throws ID, ID {
    try {
      ID ID = new ID();

      COMMENT_LINE_C
      if (ID.ID == INTEGER) throw new ID(ID.ID);
      for (ID ID : ID)
        if (ID.ID(INTEGER) == STRING_SQ)
          throw new ID(STRING_DQ + ID + STRING_DQ + ID.ID);
        else if (ID.ID(INTEGER) == STRING_SQ) {
          if (ID.ID() == INTEGER) throw new ID(STRING_DQ);
          if (ID.ID(INTEGER) == STRING_SQ) {
            if (ID.ID() == INTEGER) throw new ID(STRING_DQ);
            ID.ID(ID.ID(INTEGER)); COMMENT_LINE_C
          } else
            ID.ID(STRING_DQ + ID.ID(INTEGER)); COMMENT_LINE_C
        } else
          ID.ID(ID.ID(ID)); COMMENT_LINE_C

      COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C

      switch (ID.ID.ID) {
      case ID:

        COMMENT_LINE_C
        ID ID = ID.ID(STRING_DQ, ID.ID(), false, ID.ID);
        COMMENT_LINE_C

        ID ID = new ID(ID, ID.ID);

        COMMENT_LINE_C
        ID ID = new ID();
        ID ID = new ID(ID);

        ID.ID(ID);
        ID.ID(ID.ID.ID, ID.ID.ID + STRING_DQ,
            ID.ID()COMMENT_BLOCK_C);

        ID.ID();
        ID.ID(ID);
        ID.ID(ID.ID.ID, ID.ID.ID + STRING_DQ, ID.ID());
        break;

      case ID:
        ID ID = new ID(
            ID.ID(STRING_DQ, ID.ID(), true, ID.ID));
        COMMENT_LINE_C
        if (ID.ID.ID.ID()) throw new ID(STRING_DQ);
        ID.ID(ID.ID.ID.ID(INTEGER));
        ID.ID();
        ID.ID();
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      case ID:
        ID(ID, ID.ID, new ID(
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID))));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      case ID:

        COMMENT_LINE_C
        ID ID = ID.ID(STRING_DQ, ID.ID(), false, ID.ID);

        ID ID = new ID(ID, ID.ID);

        COMMENT_LINE_C
        ID = new ID();
        ID = new ID(ID);

        ID.ID(ID);
        ID.ID(ID.ID.ID, ID.ID.ID + STRING_DQ,
            ID.ID()COMMENT_BLOCK_C);

        ID.ID();
        ID.ID(ID.ID.ID, ID.ID.ID + STRING_DQ,
            STRING_DQ + ID.ID.ID + STRING_DQ);

        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID))));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID))));
        break;

      case ID:
        ID(ID, ID.ID,
            new ID(ID.ID(STRING_DQ, ID.ID(), true, ID.ID)));
        break;

      default:
        throw new ID(STRING_DQ + ID.ID.ID);
      }
    } catch (ID ID) {
      ID.ID(ID.ID());
    }
  }
}




public class ID extends ID {

  COMMENT_BLOCK_C
  private static final long ID = INTEGER;

  public void ID(int... ID) {
    for (int ID : ID)
      ID(ID);
  }

}







public class ID {

  private final ID ID;
  private ID ID = null;
  private final int ID = INTEGER;
  private final boolean ID = true;

  public ID(ID ID, ID ID) throws ID {
    ID = new ID(ID);
    COMMENT_LINE_C
  }

  public void ID(ID ID) throws ID {
    COMMENT_LINE_C
    ID = ID;

    COMMENT_LINE_C
    ID<ID> ID = new ID<>();

    int ID = INTEGER;
    int ID = ID.ID;

    while (++ID < ID.ID) {
      if (++ID > ID) { COMMENT_LINE_C
        while (ID.ID[--ID] != INTEGER)
          ;
        ID.ID(ID);
        ID = INTEGER;
      }
    }
    ID.ID(ID.ID);

    COMMENT_LINE_C

    COMMENT_LINE_C
    ID.ID(STRING_DQ + STRING_DQ
        + STRING_DQ + STRING_DQ);

    ID.ID(STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ
        + STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ
        + STRING_DQ + STRING_DQ + ID.ID + STRING_DQ + STRING_DQ
        + ID.ID + STRING_DQ + STRING_DQ + ID.ID + STRING_DQ);

    for (int ID = INTEGER; ID.ID[ID.ID][ID] != INTEGER; ID++)
      ID.ID(ID.ID[ID.ID][ID] + STRING_DQ);
    ID.ID(STRING_DQ);

    ID.ID(STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ);

    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    int ID = INTEGER;
    for (int ID = INTEGER; ID < ID.ID; ID++) {
      if (ID > ID.ID(ID)) {
        ID.ID(STRING_DQ + (ID + INTEGER) + STRING_DQ + (ID + INTEGER) + STRING_DQ);
        ID++;
      }
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID[ID] + STRING_DQ);
    }
    ID.ID(STRING_DQ);

    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ + ID.ID.ID + STRING_DQ);
    for (int ID = INTEGER; ID < ID.ID.ID; ID++)
      if (ID.ID[ID] != null) ID.ID(STRING_DQ + ID + STRING_DQ);
    ID.ID(STRING_DQ);

    for (int ID = INTEGER; ID < ID.ID.ID; ID++) {
      if (ID.ID[ID] != null) {
        ID.ID(STRING_DQ + ID + STRING_DQ);
        ID.ID(STRING_DQ + ID + STRING_DQ);
        ID.ID(STRING_DQ + ID + STRING_DQ + ID + STRING_DQ + ID.ID[ID].ID + STRING_DQ);
        for (int ID = INTEGER; ID < ID.ID[ID].ID; ID++)
          if (ID.ID[ID][ID]) ID.ID(STRING_DQ + ID + STRING_DQ + ID + STRING_DQ);
        ID.ID(STRING_DQ);
      }
    }

    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    for (int ID = ID.ID + INTEGER; ID < ID.ID; ID++) {
      ID.ID(STRING_DQ + ID + STRING_DQ);
      for (int ID = INTEGER; ID.ID[ID] != null && ID < ID.ID[ID].ID - INTEGER; ID++)
        ID.ID(ID.ID[ID][ID] + INTEGER + STRING_DQ);

      ID.ID(STRING_DQ + STRING_DQ + ID.ID[ID] + STRING_DQ);
      for (int ID = INTEGER; ID.ID[ID] != null && ID < ID.ID[ID].ID - INTEGER; ID++)
        ID.ID(ID.ID[ID.ID[ID][ID] + INTEGER] + STRING_DQ);
      ID.ID(STRING_DQ);
    }
    ID.ID(STRING_DQ);

    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    for (int ID = ID.ID; ID < ID.ID; ID++)
      if (ID.ID[ID] != INTEGER) ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID[ID] + STRING_DQ
          + ID.ID[ID] + STRING_DQ + ID.ID(ID.ID[ID.ID[ID]]));
    ID.ID(STRING_DQ);

    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    for (int ID = ID.ID + INTEGER; ID < ID.ID; ID++)
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID[ID] + STRING_DQ + ID.ID[ID] + STRING_DQ
          + ID.ID(ID.ID[ID.ID[ID]]));
    ID.ID(STRING_DQ);

    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    ID.ID(STRING_DQ + ID.ID + STRING_DQ);
    ID.ID(STRING_DQ + ID.ID + STRING_DQ);

    ID = INTEGER;
    for (int ID = ID.ID; ID < ID.ID; ID++) {
      if (ID > ID.ID(ID)) {
        ID.ID(STRING_DQ + (ID + INTEGER) + STRING_DQ + (ID + INTEGER) + STRING_DQ);
        ID++;
      }

      ID.ID(STRING_DQ + ID.ID[ID]);
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID[ID] + STRING_DQ + ID.ID[ID.ID[ID]]);
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID(ID) + STRING_DQ + ID.ID[ID.ID(ID)]);
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID[ID] + STRING_DQ);
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID[ID] + STRING_DQ + ID.ID[ID.ID[ID]]);
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID(ID) + STRING_DQ);
    }
    ID.ID(STRING_DQ);

    ID.ID(STRING_DQ);

    for (int ID = ID.ID() - INTEGER; ID >= INTEGER; ID--)
      ID.ID(STRING_DQ + ID.ID(ID) + STRING_DQ + (ID + INTEGER) + STRING_DQ);

    int ID = INTEGER;

    ID.ID(STRING_DQ);
    for (int ID = ID.ID; ID < ID.ID; ID++) {
      if (ID > ID.ID(ID)) ID.ID(STRING_DQ + (INTEGER + ID++) + STRING_DQ);
      if (ID.ID(ID) && ID < ID.ID - INTEGER && !ID.ID(ID + INTEGER)) continue;

      ID.ID(STRING_DQ + ID + STRING_DQ + STRING_DQ + ID.ID[ID] + (ID ? STRING_DQ : STRING_DQ));

      if (ID.ID(ID))
        ID.ID(STRING_DQ + ID.ID(ID) + STRING_DQ
            + STRING_DQ + STRING_DQ + STRING_DQ
            + ID.ID[ID.ID[ID]] + STRING_DQ + STRING_DQ + ID.ID[ID] + STRING_DQ
            + STRING_DQ);
      else {
        if (ID.ID(ID - INTEGER)) {COMMENT_LINE_C
          if (ID.ID(ID)) {

            ID.ID(STRING_DQ + (ID + INTEGER) + STRING_DQ + STRING_DQ);
          } else if (ID.ID(ID)) {
            ID.ID(STRING_DQ + (ID + INTEGER) + STRING_DQ);
          }
          if (ID.ID(ID)) {
            ID.ID(STRING_DQ + ID.ID[ID.ID[ID]] + STRING_DQ + STRING_DQ + ID.ID[ID]
                + STRING_DQ + STRING_DQ);
          }
          continue;
        } else {
          if (ID.ID(ID)) {
            ID.ID(STRING_DQ + ID.ID[ID] + STRING_DQ + ID.ID[ID] + STRING_DQ
                + ID.ID[ID.ID[ID]] + STRING_DQ);
            ID.ID(STRING_DQ + (ID + INTEGER) + STRING_DQ + STRING_DQ);
          } else if (ID.ID(ID)) {
            ID.ID(STRING_DQ + ID.ID[ID] + STRING_DQ + ID.ID[ID] + STRING_DQ
                + ID.ID[ID.ID[ID]] + STRING_DQ);
            ID.ID(STRING_DQ + (ID + INTEGER) + STRING_DQ);
          }
          if (ID.ID(ID)) {
            ID.ID(STRING_DQ + ID.ID[ID.ID[ID]] + STRING_DQ + STRING_DQ
                + ID.ID[ID] + STRING_DQ + STRING_DQ);
          }
        }
      }
    }

    ID.ID(
        STRING_DQ + STRING_DQ);
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);

  }
}


@ID(STRING_DQ)
public class ID extends ID {
  public ID(ID ID) {
    super(ID);
  }
}








public class ID extends ID {

  public static void ID(final ID[] ID) throws ID, ID {
    ID.ID(ID);
  }

  @ID
  public void ID(final ID ID) throws ID, ID {
    new ID().ID(STRING_DQ, STRING_DQ);
  }
}



COMMENT_LINE_C
public class ID {

  ID ID;

  public ID(ID ID, ID ID, ID ID, ID ID, ID ID, double ID,
      double ID, double ID, double ID) throws ID {

    this.ID = ID;
    ID ID = this; COMMENT_LINE_C
    ID ID;
    ID ID;
    ID ID;

    COMMENT_LINE_C
    ID ID = new ID(ID.ID());
    ID ID = new ID(ID.ID());
    ID.ID(true);

    ID ID = new ID();
    ID.ID(INTEGER);
    ID ID = new ID(STRING_DQ);
    ID.ID(ID -> {
      ID.ID.ID(STRING_DQ);
      ID.ID().ID();
      ID.ID().ID();
      ID.ID();
      ID.ID(() -> ID.ID(new ID()));

    });
    ID.ID(new ID(INTEGER));
    ID.ID(INTEGER);

    ID ID = new ID(STRING_DQ);
    ID.ID(ID -> {
      ID.ID.ID(STRING_DQ);
    });
    ID.ID(new ID(INTEGER));
    ID.ID(INTEGER);

    ID ID = new ID(STRING_DQ);
    ID.ID(ID -> {
      ID.ID();
      ID.ID(INTEGER);
    });
    ID.ID(new ID(INTEGER));
    ID.ID(INTEGER);

    ID.ID().ID(ID, ID, ID);

    ID ID = new ID(ID.ID(ID));
    ID.ID(INTEGER);
    ID.ID(INTEGER);
    ID ID = new ID(ID.ID(ID));
    ID.ID(INTEGER);
    ID.ID(INTEGER);

    ID ID = new ID(ID);
    ID.ID(true);
    COMMENT_LINE_C
    ID ID = new ID(ID);
    ID.ID(true);
    COMMENT_LINE_C

    ID ID = new ID();
    ID.ID().ID(ID, ID, ID);

    ID ID = new ID();

    ID.ID().ID(ID, ID);

    ID = ID.ID().ID();
    ID = new ID();
    ID.ID(
        STRING_DQ + ID == null ? STRING_DQ : ID + STRING_DQ + ID == null ? STRING_DQ : ID);
    ID.ID(ID.ID() * ID);
    ID.ID(ID.ID() * ID);
    ID.ID(ID.ID() * ID);
    ID.ID(ID.ID() * ID);
    ID.ID(new ID<ID>() {
      @ID
      public void ID(ID ID) {
        ID.ID();
        ID.ID(INTEGER);
      }
    });

    COMMENT_LINE_C
    ID = new ID(ID);
    ID.ID(ID);
    ID.ID();

    COMMENT_LINE_C
    new ID(ID.ID(), false);

  }

}





















public class ID {
  private final ID ID = new ID();
  private final ID ID = new ID();
  ID ID = new ID();
  private final ID ID = new ID(true);
  private final ID ID = new ID();
  private final ID ID = new ID();
  private final ID ID = new ID();
  private final double ID = INTEGER;

  private double ID;
  private double ID;
  private double ID;
  private double ID;
  private double ID;
  private double ID;

  private final ID ID = new ID();
  private final ID ID = new ID();
  private final ID ID = new ID();
  private final ID ID = new ID();

  COMMENT_LINE_C
  private final ID ID;
  private final boolean ID;
  private final double ID = INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = INTEGER;
  private final double ID = ID / INTEGER;
  private final double ID = INTEGER;
  private final double ID = INTEGER;
  private final double ID = INTEGER * ID;
  private final double ID = INTEGER;
  private final double ID = INTEGER;
  private final double ID = ID + INTEGER;
  private final ID ID;

  COMMENT_LINE_C
  private int ID, ID;
  private final ID<ID, ID> ID = new ID<ID, ID>();
  private final ID<ID, ID> ID = new ID<ID, ID>();

  public ID(ID ID, boolean ID) {
    COMMENT_LINE_C
    this.ID = ID;
    this.ID = ID;
    ID = ID ? ID : ID;
    final ID ID = new ID();
    ID(ID);
    ID.ID().ID(ID);
    COMMENT_LINE_C

    COMMENT_LINE_C
    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID().ID(ID);

    ID();
    ID ID = new ID(ID, INTEGER, INTEGER, true, ID.ID);
    ID.ID(ID.ID);
    ID(ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    ID.ID();
  }

  private void ID() {
    ID.ID().ID(ID);
    ID.ID().ID(ID);
    ID.ID().ID(ID);
    ID.ID().ID(ID);
    ID.ID(COMMENT_BLOCK_CINTEGER); COMMENT_LINE_C

    ID.ID(REAL);
    ID.ID(REAL);
    ID.ID(-ID);
    ID.ID.ID(REAL);
    ID.ID.ID(INTEGER);
  }

  private void ID(ID ID, final ID ID) {
    ID.ID(new ID<ID>() {
      @ID
      public void ID(ID ID) {
        ID = ID.ID();
        ID = ID.ID();
        ID = ID.ID();
        ID = ID.ID();
      }
    });
    ID.ID(new ID<ID>() {
      @ID
      public void ID(ID ID) {
        ID = ID;
        ID = ID;
        ID = ID.ID();
        ID = ID.ID();
        ID = (ID - ID);
        ID = (ID - ID);

        if (ID.ID() && (ID.ID() || ID.ID())) {
          double ID = ID.ID();
          double ID = ID + ID;
          ID.ID(ID);
        } else if (ID.ID()) {
          ID.ID.ID(ID.ID.ID() + ID);
          ID.ID.ID(ID.ID.ID() - ID);
        } else if (ID.ID()) {
          ID.ID.ID(ID.ID.ID() - ID);
          ID.ID.ID(ID.ID.ID() - ID);
        }
      }
    });
  }

  COMMENT_LINE_C
  private int ID(int ID, int ID, int ID) {
    int ID = ID;
    COMMENT_LINE_C
    COMMENT_LINE_C
    ID ID = new ID(ID.ID[ID.ID(ID)]).ID();
    double ID = ID.ID() + ID;
    double ID = ID.ID();

    if (ID.ID(ID) == null || ID > ID.ID(ID)) ID.ID(ID, ID);
    if (ID.ID(ID) == null || ID > ID.ID(ID)) ID.ID(ID, ID);

    COMMENT_LINE_C
    for (int ID = ID.ID(ID); ID != INTEGER; ID = ID.ID(ID)) {
      if (ID.ID(ID) != INTEGER) {
        int ID = ID(ID.ID(ID), ID, ID + INTEGER);
        ID = ID(ID.ID(ID), ID + INTEGER, ID + INTEGER);
      } else
        ID = ID(ID.ID(ID), ID, ID + INTEGER);
    }
    return ID;
  }

  COMMENT_LINE_C
  private void ID(ID ID, int ID, int ID, int ID, int ID, int ID, boolean ID, boolean ID, boolean ID,
      boolean ID) {
    COMMENT_LINE_C
    if (!ID) ID = ID;
    COMMENT_LINE_C
    double ID = ID.ID(ID);
    double ID = ID.ID(ID);
    double ID = ID.ID(ID + INTEGER) - ID - ID;
    double ID = ID.ID(ID + INTEGER) - ID - ID;
    double ID = ID / INTEGER;
    double ID = ID / INTEGER;

    COMMENT_LINE_C
    ID ID = new ID(ID, ID);
    ID.ID().ID(ID.ID[ID.ID(ID)], ID, ID);

    COMMENT_LINE_C
    final ID ID = new ID(ID + ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID / INTEGER);
    final ID ID = new ID(ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    final ID ID = new ID(ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    ID.ID(ID);
    final ID ID = new ID(ID + ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    ID.ID(ID);

    ID ID = null, ID = null;
    if (ID) {
      if (ID) {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(-ID);
      } else {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(-ID);
      }
      ID.ID(ID);
    }

    if (ID) {
      if (ID) {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(ID + ID);
      } else {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(ID + ID);
      }
      ID.ID(ID);
    }
    ID ID = null;
    if (ID) {
      double ID = (ID - ID) * ID;
      ID = new ID(ID, ID, ID);
      ID.ID(ID);
      ID.ID(ID);
      ID.ID(-ID);
      ID.ID(-(ID / INTEGER));
    }
    COMMENT_LINE_C
    ID ID = new ID();
    ID.ID(ID);
    ID.ID(ID);
    ID.ID(ID * ID);
    ID.ID().ID(ID, ID, ID, ID, ID);
    if (ID) ID.ID().ID(ID);
    if (ID) ID.ID().ID(ID);
    if (ID) ID.ID().ID(ID);

    ID.ID().ID(ID);
  }

  int ID = INTEGER;

  COMMENT_LINE_C
  private int ID(ID ID, int ID, int ID, int ID, int ID, int ID, boolean ID, boolean ID) {
    int ID = ID;
    int ID = ID;

    COMMENT_LINE_C

    COMMENT_LINE_C
    int ID = ID;
    for (int ID = ID.ID(ID); ID != INTEGER; ID = ID.ID(ID)) {
      if (ID.ID(ID) != INTEGER) {
        ID = ID(ID, ID.ID(ID), ID, ID, ID + INTEGER, ID, true, ID != ID);
        ID = ID(ID, ID.ID(ID), ID, ID + INTEGER, ID + INTEGER, ID, true, false);
      } else
        ID = ID(ID, ID.ID(ID), ID, ID, ID + INTEGER, ID, true, ID != ID);

      if (ID.ID(ID) != INTEGER) ID++;
    }

    ID(ID, ID, ID, ID, ID, ID, ID.ID(ID) != INTEGER, ID, ID >= INTEGER, ID);

    final ID ID;
    if (ID != ID && ID != INTEGER) {
      if (ID) {
        double ID = ID.ID(ID);
        ID = new ID(ID, ID + ID, ID);
        ID.ID(ID.ID(ID) + ID + ID / INTEGER);
        ID.ID(ID.ID(ID) - ID);
      } else {
        double ID = ID.ID(ID) - ID.ID(ID);
        ID = new ID(ID + ID, ID, ID);
        ID.ID(ID.ID(ID) + ID + ID / INTEGER);
        ID.ID(ID.ID(ID) - ID);
        ID.ID(ID * ID);
      }
      ID.ID(ID);
      ID.ID().ID(ID);
    }
    return ID;
  }

  private void ID(ID ID) {
    COMMENT_LINE_C
    ID(ID.ID(), INTEGER, INTEGER);

    COMMENT_LINE_C
    ID = ID = INTEGER;
    for (int ID : ID.ID())
      if (ID > ID) ID = ID;
    for (int ID : ID.ID())
      if (ID > ID) ID = ID;

    COMMENT_LINE_C
    double ID = INTEGER;
    for (int ID = INTEGER; ID <= ID; ID++) {
      ID ID = ID.ID(ID);
      ID.ID(ID, ID);
      ID += ID + ID;
    }
    ID.ID(ID + INTEGER, ID);

    ID = INTEGER;
    for (int ID = INTEGER; ID <= ID; ID++) {
      ID ID = ID.ID(ID);
      ID.ID(ID, ID);
      ID += ID + ID;
    }
    ID.ID(ID + INTEGER, ID);

    COMMENT_LINE_C
    COMMENT_LINE_C
    COMMENT_LINE_C
    ID(ID, ID.ID(), -INTEGER, INTEGER, INTEGER, INTEGER, false, false);

    COMMENT_LINE_C
    ID.ID(-ID.ID(ID) / INTEGER);
    ID.ID(-ID.ID(ID) / INTEGER);
  }
}









class ID {
  final ID ID;
  final double ID = INTEGER; COMMENT_LINE_C
  final double ID = INTEGER; COMMENT_LINE_C
  final double ID = ID / INTEGER; COMMENT_LINE_C
  final double ID = INTEGER; COMMENT_LINE_C
  final double ID = INTEGER * ID; COMMENT_LINE_C
  final double ID = INTEGER; COMMENT_LINE_C
  final double ID = new ID(STRING_DQ).ID().ID() - INTEGER;

  int ID, ID;
  ID<ID, ID> ID = new ID<ID, ID>();
  ID<ID, ID> ID = new ID<ID, ID>();
  private final ID ID;

  private double ID(ID ID) {
    return new ID(ID.ID()).ID().ID() + ID;
  }

  private void ID(ID ID, ID ID, int ID, int ID, boolean ID, boolean ID) {
    double ID = ID.ID(ID);
    double ID = ID.ID(ID);
    double ID = ID.ID(ID + INTEGER) - ID - ID + ID;
    double ID = ID.ID(ID + INTEGER) - ID - ID + ID;

    ID.ID(ID, ID, ID, ID, ID, ID);
    ID.ID(ID.ID(), ID + ID, ID + ID); COMMENT_LINE_C
    ID.ID(ID + (ID ? INTEGER : INTEGER), ID, ID + INTEGER, ID - ID + INTEGER);
    if (ID) ID.ID(ID + INTEGER, ID + ID, ID + INTEGER, ID + ID + ID);
    COMMENT_LINE_C
  }

  private void ID(ID ID, int ID, int ID) {
    COMMENT_LINE_C
    if (ID > ID) ID = ID;
    if (ID > ID) ID = ID;
    ID ID = new ID(ID.ID()).ID();

    double ID = ID.ID();
    double ID = ID.ID();
    if (ID.ID(ID) == null || ID > ID.ID(ID)) ID.ID(ID, ID);
    if (ID.ID(ID) == null || ID > ID.ID(ID)) ID.ID(ID, ID);
    if (ID.ID() != null) ID(ID.ID(), ID, ID + INTEGER);
    if (ID.ID() != null) ID(ID.ID(), ID + INTEGER, ID);
  }

  private void ID(ID ID, ID ID, int ID, int ID, int ID, boolean ID) {
    COMMENT_LINE_C
    if (ID > ID) ID = ID;
    if (ID > ID) ID = ID;
    COMMENT_LINE_C
    int ID = INTEGER;
    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID++;

    boolean ID = ID != INTEGER;
    if (ID == INTEGER) COMMENT_LINE_C
      ID = INTEGER;

    ID(ID, ID, ID, ID, ID, ID);

    if (ID.ID() != null) ID(ID.ID(), ID, ID, ID, ID + INTEGER, true);
    if (ID.ID() != null)
      ID(ID.ID(), ID, ID, ID + INTEGER, ID, false);
    else COMMENT_LINE_C
    if (ID != ID && ID != INTEGER)
      ID.ID(ID.ID(ID) + INTEGER, ID.ID(ID) - ID, ID.ID(ID) + INTEGER, ID.ID(ID) - ID);
  }

  ID(ID ID) {
    this.ID = ID;

    COMMENT_LINE_C
    ID = ID = INTEGER;
    ID(ID.ID(), INTEGER, INTEGER);

    COMMENT_LINE_C
    double ID = INTEGER;
    for (int ID = INTEGER; ID <= ID; ID++) {
      ID ID = ID.ID(ID);
      ID.ID(ID, ID);
      ID += ID + ID;
    }
    ID.ID(ID + INTEGER, ID);
    COMMENT_LINE_C

    ID = INTEGER;
    for (int ID = INTEGER; ID <= ID; ID++) {
      ID ID = ID.ID(ID);
      ID.ID(ID, ID);
      ID += ID + ID;
    }
    ID.ID(ID + INTEGER, ID);
    COMMENT_LINE_C

    COMMENT_LINE_C
    ID = new ID(ID.ID(ID + INTEGER), ID.ID(ID + INTEGER));
    ID ID = ID.ID();
    ID.ID(ID.ID);

    ID ID = new ID(ID);
    ID.ID(true);
    ID.ID(true);
    ID.ID(true);

    COMMENT_LINE_C
    ID = ID = INTEGER;
    ID(ID.ID(), ID, -INTEGER, INTEGER, INTEGER, false);
  }

  public ID ID() {
    return ID;
  }
}






















public class ID {
  private final ID ID = new ID();
  private final ID ID = new ID();
  ID ID = new ID();
  private final ID ID = new ID(true);
  private final ID ID = new ID();
  private final ID ID = new ID();
  private final ID ID = new ID();
  private final double ID = INTEGER;

  private double ID;
  private double ID;
  private double ID;
  private double ID;
  private double ID;
  private double ID;

  private final ID ID = new ID();
  private final ID ID = new ID();
  private final ID ID = new ID();
  private final ID ID = new ID();

  COMMENT_LINE_C
  private final ID ID;
  private final boolean ID;
  private final double ID = INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = ID / INTEGER; COMMENT_LINE_C
  private final double ID = INTEGER;
  private final double ID = INTEGER;
  private final double ID = INTEGER * ID;
  private final double ID = INTEGER;
  private final double ID = INTEGER;
  private final double ID = ID + INTEGER;
  private final ID ID;

  COMMENT_LINE_C
  private int ID = INTEGER, ID = INTEGER; COMMENT_LINE_C
  private final ID<ID, ID> ID = new ID<ID, ID>();
  private final ID<ID, ID> ID = new ID<ID, ID>();

  public ID(ID ID, boolean ID) {
    COMMENT_LINE_C
    this.ID = ID;
    this.ID = ID;
    ID = ID ? ID : ID;
    final ID ID = new ID();
    ID(ID);
    ID.ID().ID(ID);
    COMMENT_LINE_C

    COMMENT_LINE_C
    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID(ID.ID);
    ID.ID(ID.ID);

    ID.ID().ID(ID);

    ID();
    ID ID = new ID(ID, INTEGER, INTEGER, true, ID.ID);
    ID.ID(ID.ID);
    ID(ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    ID.ID();
  }

  private void ID() {
    ID.ID().ID(ID);
    ID.ID().ID(ID);
    ID.ID().ID(ID);
    ID.ID().ID(ID);
    ID.ID(COMMENT_BLOCK_CINTEGER); COMMENT_LINE_C

    ID.ID(REAL);
    ID.ID(REAL);
    ID.ID(-ID);
    ID.ID.ID(REAL);
    ID.ID.ID(INTEGER);
  }

  private void ID(ID ID, final ID ID) {
    ID.ID(new ID<ID>() {
      @ID
      public void ID(ID ID) {
        ID = ID.ID();
        ID = ID.ID();
        ID = ID.ID();
        ID = ID.ID();
      }
    });
    ID.ID(new ID<ID>() {
      @ID
      public void ID(ID ID) {
        ID = ID;
        ID = ID;
        ID = ID.ID();
        ID = ID.ID();
        ID = (ID - ID);
        ID = (ID - ID);

        if (ID.ID()) {
          ID.ID.ID(ID.ID.ID() + ID);
          ID.ID.ID(ID.ID.ID() - ID);
        } else if (ID.ID()) {
          double ID = ID.ID();
          double ID = ID + ID;
          ID.ID(ID);
        } else if (ID.ID()) {
          ID.ID.ID(ID.ID.ID() - ID);
          ID.ID.ID(ID.ID.ID() - ID);
        }
      }
    });
  }

  private void ID(ID ID, int ID, int ID) {
    COMMENT_LINE_C
    if (ID > ID) ID = ID;
    if (ID > ID) ID = ID;
    ID ID = new ID(ID.ID()).ID();

    double ID = ID.ID() + ID;
    double ID = ID.ID();
    if (ID.ID(ID) == null || ID > ID.ID(ID)) ID.ID(ID, ID);
    if (ID.ID(ID) == null || ID > ID.ID(ID)) ID.ID(ID, ID);

    if (ID.ID() != null) ID(ID.ID(), ID ? ID + INTEGER : ID, ID ? ID : ID + INTEGER);
    if (ID.ID() != null) ID(ID.ID(), ID ? ID : ID + INTEGER, ID ? ID + INTEGER : ID);
  }

  COMMENT_LINE_C
  private void ID(ID ID, ID ID, int ID, int ID, boolean ID, boolean ID, boolean ID) {
    COMMENT_LINE_C
    COMMENT_LINE_C
    double ID = ID.ID(ID);
    double ID = ID.ID(ID);
    double ID = ID.ID(ID + INTEGER) - ID - ID;
    double ID = ID.ID(ID + INTEGER) - ID - ID;
    double ID = ID / INTEGER;
    double ID = ID / INTEGER;

    COMMENT_LINE_C
    ID ID = new ID(ID, ID);
    ID.ID().ID(ID.ID(), ID, ID);

    COMMENT_LINE_C
    final ID ID = new ID(ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID / INTEGER);
    final ID ID = new ID(ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    final ID ID = new ID(ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    ID.ID(ID);
    final ID ID = new ID(ID, ID, ID);
    ID.ID(ID);
    ID.ID(ID);
    ID.ID(ID);

    final ID ID, ID;
    if (ID) {
      if (ID) {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(-ID);
      } else {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(-ID);
      }
      ID.ID(ID);
    } else
      ID = null;

    if (ID) {
      if (ID) {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(ID + ID);
      } else {
        ID = new ID(ID, ID, ID);
        ID.ID(ID);
        ID.ID(ID + ID);
      }
      ID.ID(ID);
    } else
      ID = null;

    COMMENT_LINE_C
    ID ID = new ID();
    ID.ID(ID);
    ID.ID(ID);
    ID.ID().ID(ID, ID, ID, ID, ID);
    if (ID) ID.ID().ID(ID);
    if (ID) ID.ID().ID(ID);

    ID.ID().ID(ID);
  }

  private void ID(ID ID, ID ID, int ID, int ID, int ID, boolean ID) {
    if (ID > ID) ID = ID;
    if (ID > ID) ID = ID;
    COMMENT_LINE_C
    int ID = INTEGER;
    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID++;

    boolean ID = ID != INTEGER;
    if (ID == INTEGER) COMMENT_LINE_C
      ID = INTEGER;

    ID(ID, ID, ID, ID, ID, ID, ID >= INTEGER);

    if (ID.ID() != null)
      ID(ID, ID.ID(), ID ? ID : ID, ID ? ID + INTEGER : ID, ID ? ID : ID + INTEGER, true);
    if (ID.ID() != null)
      ID(ID, ID.ID(), ID, ID ? ID : ID + INTEGER, ID ? ID + INTEGER : ID, false);
    else {COMMENT_LINE_C
      final ID ID;
      if (ID != ID && ID != INTEGER) {
        if (ID) {
          double ID = ID.ID(ID) - ID.ID(ID);
          ID = new ID(ID, ID, ID);
          ID.ID(ID.ID(ID) + ID + ID / INTEGER);
          ID.ID(ID.ID(ID) - ID);
        } else {
          double ID = ID.ID(ID) - ID.ID(ID);
          ID = new ID(ID, ID, ID);
          ID.ID(ID.ID(ID) + ID + ID / INTEGER);
          ID.ID(ID.ID(ID) - ID);
        }
        ID.ID(ID);
        ID.ID().ID(ID);
      }
    }
  }

  private void ID(ID ID) {
    COMMENT_LINE_C
    ID = ID = INTEGER;
    ID(ID.ID(), INTEGER, INTEGER);

    COMMENT_LINE_C
    double ID = INTEGER;
    for (int ID = INTEGER; ID <= ID; ID++) {
      ID ID = ID.ID(ID);
      ID.ID(ID, ID);
      ID += ID + ID;
    }
    ID.ID(ID + INTEGER, ID);

    ID = INTEGER;
    for (int ID = INTEGER; ID <= ID; ID++) {
      ID ID = ID.ID(ID);
      ID.ID(ID, ID);
      ID += ID + ID;
    }
    ID.ID(ID + INTEGER, ID);

    COMMENT_LINE_C
    COMMENT_LINE_C
    COMMENT_LINE_C
    ID = ID = INTEGER;
    ID(ID, ID.ID(), -INTEGER, INTEGER, INTEGER, false);

    COMMENT_LINE_C
    ID.ID(-ID.ID(ID) / INTEGER);
    ID.ID(-ID.ID(ID) / INTEGER);
  }
}


public enum ID {
  ID, ID, ID, ID, ID;

  @ID
  public ID ID() {
    switch (this) {
    case ID:
      return STRING_DQ;
    case ID:
      return STRING_DQ;
    case ID:
      return STRING_DQ;
    case ID:
      return STRING_DQ;
    case ID:
      return STRING_DQ;
    default:
      return STRING_DQ;
    }
  }
}




public class ID {
  boolean ID = false;

  ID ID;
  ID ID;
  ID ID;

  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;
  int ID = INTEGER;

  private ID ID;

  public ID(ID ID, ID ID) throws ID {
    this.ID = ID;
    this.ID = ID;
  }

  public void ID(ID ID) throws ID {
    COMMENT_LINE_C
    COMMENT_LINE_C
    ID(new ID(new ID()));
    ID(ID);
  }

  private void ID(ID ID) throws ID {
    ID = new ID(ID);
    ID.ID(INTEGER);
    ID.ID(ID.ID + STRING_DQ, ID.ID);
    for (ID ID : ID.ID())
      ID.ID().ID(STRING_DQ, ID);
    ID.ID(ID.ID, STRING_DQ);
    ID.ID();
    ID();
    ID();
    ID();
    ID();
    ID(ID.ID());
    ID();
    ID(ID.ID);
    if (ID.ID().ID(STRING_DQ) && ID.ID == ID.ID) { COMMENT_LINE_C
                                                                                                                    COMMENT_LINE_C
      COMMENT_LINE_C
      ID.ID();
      ID.ID().ID(STRING_DQ);
      ID.ID();
      ID.ID().ID(STRING_DQ);
      if (!ID.ID) for (ID ID : ID.ID())
        ID.ID().ID(STRING_DQ, ID);
      ID();
    }
    ID.ID();
    ID.ID(ID.ID);
    ID.ID(ID.ID + STRING_DQ, ID.ID);
  }

  public void ID(ID ID) {
    ID = new ID(ID);
    ID.ID(INTEGER);
    ID.ID(ID.ID + STRING_DQ, ID.ID);
    ID.ID(ID.ID, STRING_DQ);

    ID();
    ID();
    ID.ID(ID.ID);
    ID.ID(ID.ID + STRING_DQ, ID.ID);
  }

  ID ID() {
    if (ID.ID)
      return STRING_DQ;
    else
      return STRING_DQ;
  }

  void ID(ID ID, ID ID) throws ID {
    if (ID != null) {
      ID.ID();
      ID.ID(ID);
    }
    switch (ID.ID) {
    case ID:
    case ID:
      ID.ID(ID);
      break;

    case ID:
    case ID:
      ID.ID(ID, true);
      break;

    default:
      throw new ID(STRING_DQ);
    }
  }

  void ID(ID ID) throws ID {
    switch (ID.ID) {
    case ID:
      ID.ID(ID);
      break;
    case ID:
      ID.ID(ID);
      break;
    case ID:
      ID.ID(ID);
      break;
    case ID:
      ID.ID(ID);
      break;
    default:
      throw new ID(STRING_DQ);
    }
  }

  void ID(ID ID) {
    if (ID.ID)
      ID.ID(STRING_DQ, ID, STRING_DQ, ID(), STRING_DQ);
    else if (ID.ID)
      ID.ID(STRING_DQ, STRING_DQ, ID(), STRING_DQ);
    else
      ID.ID(STRING_DQ, STRING_DQ, ID(), STRING_DQ);
  }

  ID ID() {
    COMMENT_LINE_C
    if (ID.ID)
      return STRING_DQ;
    else
      return STRING_DQ;
  }

  ID ID() {
    if (ID.ID)
      return STRING_DQ;
    else if (ID.ID)
      return STRING_DQ;
    else
      return STRING_DQ;
  }

  ID ID() {
    if (ID.ID)
      return STRING_DQ;
    else
      return STRING_DQ; COMMENT_LINE_C
  }

  ID ID() {
    if (ID.ID)
      return STRING_DQ;
    else
      return STRING_DQ;
  }

  ID ID() {
    if (ID.ID == ID.ID)
      return ID.ID();
    else
      return STRING_DQ;
  }

  void ID(ID ID) {
    COMMENT_LINE_C
    if (ID.ID || ID.ID || ID.ID || ID.ID) {
      COMMENT_LINE_C
      if (ID.ID != ID) ID.ID.ID = true;

      if (ID.ID != ID) ID.ID.ID = true;
    }

    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID);
  }

  void ID(ID ID) throws ID {
    COMMENT_LINE_C
    if (ID.ID) {
      ID.ID(ID.ID(STRING_DQ), true);
      ID(ID.ID(STRING_DQ));
    }

    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID);
  }

  void ID() throws ID {
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);
    ID((ID) ID.ID().ID());
    ID.ID(STRING_DQ, true);
    ID(STRING_DQ);
    ID.ID();
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ, true);
  }

  void ID(ID ID, ID ID) throws ID {
    COMMENT_LINE_C

    COMMENT_BLOCK_C
    if (ID instanceof ID)
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID(ID, null);

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      if (ID.ID == ID.ID) {
        ID.ID(ID.ID(STRING_DQ));
        ID.ID(STRING_DQ);
        ID.ID();
      }

      ID++;
      ID.ID = true;
      ID(ID.ID(STRING_DQ), STRING_DQ + ID.ID() + STRING_DQ);

      COMMENT_BLOCK_C
      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {
        ID ID = ID.ID();

        if (!ID.ID) {
          ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
          ID.ID();
          ID.ID();
        }
        ID.ID(ID(), ID.ID.ID(STRING_DQ), STRING_DQ, ID(), STRING_DQ);
        if (!ID.ID) ID.ID();
        ID.ID.ID = true;
      }
      ID.ID();
      ID(STRING_DQ);

      COMMENT_BLOCK_C
      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {

        ID.ID.ID = true;
        ID(ID.ID.ID(STRING_DQ), STRING_DQ + ID.ID().ID() + STRING_DQ);

        ID(ID, ID); COMMENT_LINE_C

        if (!(ID.ID && ID.ID)) {
          if (!ID.ID) {
            ID.ID(STRING_DQ + ID.ID().ID(ID.ID), ID.ID());
            ID(STRING_DQ);
          }

          ID(ID.ID(STRING_DQ));
          ID.ID();
          ID(STRING_DQ);
        }
      }
      if (ID.ID == ID.ID) {
        ID.ID(STRING_DQ, true);
        ID.ID(ID.ID(STRING_DQ));
        ID.ID();
      }
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      ID++;
      ID.ID();
      ID.ID(STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID.ID = true;
      ID.ID(STRING_DQ, STRING_DQ, ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ);
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      COMMENT_LINE_C
      COMMENT_LINE_C

      ID++;
      ID.ID();
      ID.ID(STRING_DQ);

      if (ID.ID) { COMMENT_LINE_C
        ID.ID();
        ID.ID();
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID.ID()), ID.ID());
        ID.ID();
        ID.ID();
        ID.ID();

        ID.ID = true;
        ID.ID(STRING_DQ, STRING_DQ, ID.ID().ID(null),
            STRING_DQ, ID.ID());
        ID.ID.ID = true;
        ID.ID(STRING_DQ, STRING_DQ, ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ);

        ID.ID.ID = true;
        ID.ID(ID(), ID.ID.ID(STRING_DQ), STRING_DQ, ID.ID(), STRING_DQ);

        ID.ID();
        ID.ID();
        ID.ID();
        ID.ID();

        ID.ID();
        ID(STRING_DQ);

        ID.ID();
        ID.ID();

        ID(ID.ID.ID(STRING_DQ), null);
      } else {
        ID.ID = true;
        COMMENT_LINE_C
        COMMENT_LINE_C
        ID.ID(STRING_DQ, ID.ID());
        ID.ID(STRING_DQ, STRING_DQ, ID.ID().ID(null),
            STRING_DQ, ID.ID());
        ID.ID(STRING_DQ, ID.ID());

        ID.ID.ID = true;
        ID.ID(STRING_DQ, STRING_DQ, ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ);
      }
      ID.ID();
      ID.ID(STRING_DQ);
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      ID++;
      ID.ID();
      ID.ID(STRING_DQ);

      ID.ID.ID = true;
      ID.ID(STRING_DQ, ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ,
          STRING_DQ);

      ID.ID();
      ID(((ID) ID.ID()).ID.ID(STRING_DQ));

      ID.ID.ID = true;

      ID(ID.ID.ID(STRING_DQ), null);
      ID.ID();
      ID.ID(STRING_DQ);
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      ID.ID();
      ID.ID(STRING_DQ);
      ID++;
      ID(ID.ID(), ID);
      ID.ID();
      ID.ID(STRING_DQ);
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      ID ID = ID.ID();

      if (ID.ID.ID(ID.ID())) {
        ID++;

        boolean ID = true;
        if (ID) {
          COMMENT_BLOCK_C
          COMMENT_LINE_C

          ID.ID();
          ID.ID(STRING_DQ);
          ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
          ID.ID();
          ID.ID.ID(STRING_DQ);

          COMMENT_LINE_C
          COMMENT_LINE_C
          COMMENT_LINE_C

          ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
          ID.ID.ID = true;
          ID.ID(STRING_DQ, STRING_DQ, ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ);

          ID.ID(ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
          ID.ID = true;
          ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
          ID.ID = true;
          ID.ID();

          ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
          ID(STRING_DQ);

          ID(ID, ID);

          if (!ID.ID) {
            ID.ID = true;
            ID.ID(ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
            ID.ID = true;
            ID(STRING_DQ);
          }
          ID.ID.ID = true;

          ID(ID.ID.ID(STRING_DQ), null);

          ID.ID();
          ID.ID(STRING_DQ);
          COMMENT_BLOCK_C
        } else {
          ID.ID();
          ID.ID(STRING_DQ);
          ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
          ID.ID();
          ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
          ID.ID.ID = true;
          ID.ID(STRING_DQ, STRING_DQ, ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ);
          ID.ID(ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
          if (!ID.ID) {
            ID.ID = true;
            ID.ID(ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
            ID(STRING_DQ);
          }
          ID.ID();

          ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
          ID(STRING_DQ);

          ID(ID, ID);

          if (!ID.ID) {
            ID.ID = true;
            ID.ID(ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
            ID(STRING_DQ);
          }
          ID.ID.ID = true;

          ID(ID.ID.ID(STRING_DQ), null);

          ID.ID();
          ID.ID(STRING_DQ);
        }
      }
      COMMENT_BLOCK_C
      else {
        ID++;

        ID.ID();
        ID.ID(STRING_DQ);
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
        ID.ID();
        ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
        ID.ID.ID = true;
        ID.ID(STRING_DQ, STRING_DQ, ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ);
        ID.ID(ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
        ID.ID();
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
        ID(STRING_DQ);
        ID(ID, ID);
        ID.ID.ID = true;

        ID(ID.ID.ID(STRING_DQ), null);
        ID.ID();
        ID.ID(STRING_DQ);
      }
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      ID ID = ID.ID();
      if (!ID.ID.ID(ID.ID())) {
        ID++;

        ID.ID();
        ID.ID(STRING_DQ);

        ID.ID.ID = true;
        ID.ID(STRING_DQ, ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ,
            STRING_DQ);
        ID.ID(STRING_DQ, STRING_DQ);
        ID(ID.ID(STRING_DQ), null);
        ID.ID = true;
        if (!ID.ID.ID(ID.ID())) {
          ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
          ID(STRING_DQ);
          ID(ID, ID);
          ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
          ID(STRING_DQ);
          ID.ID();
          ID(ID.ID(STRING_DQ));
          ID.ID.ID = true;
        }

        ID(ID.ID.ID(STRING_DQ), null);

        ID.ID();
        ID.ID(STRING_DQ);
      }
      COMMENT_BLOCK_C
      else {
        ID++;

        ID.ID();
        ID.ID(STRING_DQ);

        COMMENT_LINE_C
        ID.ID.ID = true;
        ID.ID(STRING_DQ, ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ,
            STRING_DQ);

        COMMENT_LINE_C
        ID.ID(STRING_DQ, STRING_DQ);

        COMMENT_LINE_C
        ID(ID.ID(STRING_DQ), null);
        ID.ID = true;

        COMMENT_LINE_C
        ID.ID = true;
        ID.ID(ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
        ID(STRING_DQ);

        COMMENT_LINE_C
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
        ID(STRING_DQ);

        COMMENT_LINE_C
        ID(ID, ID);

        COMMENT_LINE_C
        COMMENT_LINE_C
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
        ID(STRING_DQ);
        ID.ID();
        ID(ID.ID(STRING_DQ));
        ID.ID.ID = true;

        COMMENT_LINE_C
        ID(ID.ID.ID(STRING_DQ), null);

        ID.ID();
        ID.ID(STRING_DQ);
      }
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID)

    {
      ID ID = ID.ID();
      if (!ID.ID.ID(ID.ID())) {
        ID++;

        ID.ID();
        ID.ID(STRING_DQ);

        ID.ID.ID = true;
        ID.ID(STRING_DQ, ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ,
            STRING_DQ);
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
        ID.ID();
        ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
        ID(STRING_DQ);
        ID.ID();
        ID.ID(STRING_DQ, STRING_DQ);
        ID(ID.ID(STRING_DQ), STRING_DQ);
        ID.ID = true;
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
        ID(STRING_DQ);
        ID(ID, ID);
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
        ID(STRING_DQ);
        ID.ID();
        ID(ID.ID(STRING_DQ));
        ID.ID.ID = true;
        ID(ID.ID.ID(STRING_DQ), null);

        ID.ID();
        ID.ID(STRING_DQ);

      }
      COMMENT_BLOCK_C
      else {
        ID++;

        ID.ID();
        ID.ID(STRING_DQ);
        COMMENT_LINE_C
        ID.ID.ID = true;
        ID.ID(STRING_DQ, ID(), ID.ID.ID(STRING_DQ), STRING_DQ, STRING_DQ,
            STRING_DQ);

        COMMENT_LINE_C
        ID.ID(STRING_DQ, STRING_DQ, ID.ID.ID(STRING_DQ), ID.ID.ID(STRING_DQ),
            STRING_DQ);

        COMMENT_LINE_C
        ID(ID.ID(STRING_DQ), null);
        ID.ID = true;

        COMMENT_LINE_C
        ID.ID = true;
        ID.ID(ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
        ID(STRING_DQ);

        COMMENT_LINE_C
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
        ID(STRING_DQ);

        COMMENT_LINE_C
        ID(ID, ID);

        COMMENT_LINE_C
        ID.ID(STRING_DQ + ID.ID().ID(ID.ID().ID()), ID.ID());
        ID(STRING_DQ);

        COMMENT_LINE_C
        ID.ID();
        ID(ID.ID(STRING_DQ));
        ID.ID = true;

        COMMENT_LINE_C
        ID(ID.ID.ID(STRING_DQ), null);

        ID.ID();
        ID.ID(STRING_DQ);

      }
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      ID++;

      ID.ID();
      ID.ID(STRING_DQ);

      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {
        if (ID.ID && ID.ID) {
          if (!ID.ID) {
            ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
            ID(STRING_DQ);
          }

          ID(ID.ID(STRING_DQ));
          ID.ID();
          ID(STRING_DQ);
        } else
          ID(ID, ID);
        COMMENT_LINE_C
      }
      ID.ID();
      ID.ID(STRING_DQ);
    }

    COMMENT_BLOCK_C
    else if (ID instanceof ID) {
      ID++;

      ID.ID();
      ID.ID(STRING_DQ);

      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {
        ID ID = ID.ID(ID);

        ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
        ID.ID(ID(), ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
        ID.ID = ID.ID = true; COMMENT_BLOCK_C
      }

      ID.ID();
      ID(STRING_DQ);

      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {
        ID ID = ID.ID(ID);

        ID.ID = true;
        ID(ID.ID(STRING_DQ), null);
        ID(ID, ID);
        if (!(ID.ID && ID.ID)) {
          ID.ID();
          ID(ID.ID(STRING_DQ));
          ID.ID = true;
        }
      }

      if (!(ID.ID && ID.ID)) { COMMENT_LINE_C
        ID.ID = true;
        ID(ID.ID(STRING_DQ), null);
        if (!ID.ID) {
          ID.ID(ID.ID(STRING_DQ), STRING_DQ, STRING_DQ, STRING_DQ);
          ID(STRING_DQ);
        }
      }
      ID.ID();
      ID.ID(STRING_DQ);
    }

    else if (ID instanceof ID) {
      if (ID.ID) {

        ID.ID(STRING_DQ + ID.ID().ID(ID.ID()), ID.ID());
        ID(STRING_DQ);
      }
    }

    else {
      throw new ID(STRING_DQ + ID + STRING_DQ);
    }
  }

  void ID() {
    if (ID.ID > INTEGER) ID.ID.ID(
        STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ
            + STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ
            + STRING_DQ + STRING_DQ + STRING_DQ,
        ID, ID, ID, ID, ID, ID,
        ID, ID, ID, ID, ID,
        ID, ID);
  }

  void ID() {
    ID[] ID = ID.ID().ID().ID();
    ID.ID.ID.ID(ID);

    for (int ID = INTEGER; ID < ID.ID; ID++)
      ID.ID(STRING_DQ + ID[ID]);
  }

  void ID(ID ID, boolean ID) {
    if (ID == null) return;
    COMMENT_LINE_C

    COMMENT_LINE_C

    COMMENT_LINE_C
    if (ID) {
      if (ID.ID) ID.ID(ID.ID(STRING_DQ));
    } else {
      if (ID.ID || ID.ID || ID.ID || ID.ID || ID.ID)
        if (!ID.ID) ID.ID(ID.ID(STRING_DQ));

      if (ID.ID) ID.ID(ID.ID(STRING_DQ));

      if (ID.ID) ID.ID(ID.ID(STRING_DQ));

      if (ID.ID) ID.ID(ID.ID(STRING_DQ));

      if (ID.ID) ID.ID(ID.ID(STRING_DQ));
    }

    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID, ID);
  }

  void ID() {
    ID.ID();
    ID.ID(STRING_DQ);
    ID.ID(ID.ID().ID(STRING_DQ));

    for (ID ID : ID.ID()) {
      if (ID instanceof ID) {
        COMMENT_LINE_C
        ID.ID(ID.ID(null));
      }
    }

    for (ID ID : ID.ID()) {
      if (!(ID instanceof ID) && !(ID instanceof ID)) {
        COMMENT_LINE_C
        if (ID == null) ID = ID;
        ID.ID(ID.ID(null));
      }
    }

    COMMENT_LINE_C

    ID.ID(ID.ID().ID(STRING_DQ));

    COMMENT_LINE_C
    for (ID ID : ID.ID()) {
      if (ID instanceof ID) {
        COMMENT_LINE_C
        if (ID == null) ID = ID;
        ID.ID(ID.ID(null));
      }
    }

    for (ID ID : ID.ID()) {
      COMMENT_LINE_C
      ID.ID(ID.ID(null));
    }

    for (ID ID : ID.ID())
      COMMENT_LINE_C
      ID.ID(ID.ID(STRING_DQ));

    for (ID ID : ID.ID()) {
      for (ID ID : ID.ID()) {
        ID(ID, true); COMMENT_LINE_C
        ID(ID, false); COMMENT_LINE_C
      }
    }
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);
  }

  void ID() {
    COMMENT_LINE_C
    ID.ID();
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);
    COMMENT_LINE_C
    COMMENT_LINE_C
    COMMENT_LINE_C
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);
  }

  void ID() {
    ID.ID(STRING_DQ);
    for (ID ID : ID.ID())
      if (ID instanceof ID) ID.ID(ID.ID(), ID.ID + STRING_DQ + ID.ID(null));
    ID.ID(STRING_DQ);
    ID.ID();
  }

  void ID() {
    ID.ID(STRING_DQ);
    for (ID ID : ID.ID())
      if (ID instanceof ID) ID.ID(ID.ID());

    ID.ID(STRING_DQ);
    ID.ID();
  }

  void ID() throws ID {
    for (ID ID : ID.ID())
      ID(ID.ID, null);
  }

  void ID() throws ID {
    if (ID.ID == ID.ID) {
      COMMENT_LINE_C
      for (ID ID : ID.ID())
        ID.ID(STRING_DQ + ID.ID());

      COMMENT_LINE_C
      ID();
    }

    ID.ID(STRING_DQ, ID.ID(), STRING_DQ, ID.ID(), STRING_DQ);

    ID.ID(STRING_DQ, STRING_DQ);

    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);

    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, ID(), STRING_DQ, ID(), STRING_DQ, ID());
    ID.ID(STRING_DQ, ID());
    if (ID.ID) ID.ID(STRING_DQ);

    COMMENT_BLOCK_C
    COMMENT_LINE_C
    COMMENT_LINE_C
    if (ID.ID == ID.ID || ID.ID == ID.ID
        || ID.ID == ID.ID) {
      if (ID.ID)
        ID.ID(STRING_DQ, STRING_DQ);
      else
        ID.ID(STRING_DQ, STRING_DQ);
    }
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);

    ID.ID().ID.ID = true;
    if (ID.ID == ID.ID) {
      ID.ID();
      ID(STRING_DQ);
      ID.ID();
    }

    if (ID.ID == ID.ID) {
      ID.ID();
      ID(ID.ID().ID.ID(STRING_DQ));
      ID.ID();
    }

    if (ID.ID == ID.ID) {
      ID.ID(STRING_DQ);
      ID.ID(ID.ID == ID.ID ? STRING_DQ : STRING_DQ);
      ID.ID();
    }

    if (ID.ID == ID.ID) {
      ID.ID(null);
      ID.ID(ID.ID == ID.ID ? STRING_DQ : STRING_DQ);
      ID.ID();
      for (ID ID : ID.ID())
        if (ID.ID != null) {
          ID(ID.ID.ID(STRING_DQ), null);
          ID.ID(ID.ID(STRING_DQ));
          ID.ID();
        }
    } else
      ID();

    ID(STRING_DQ, null);
    ID.ID(STRING_DQ);
    ID.ID();
    ID.ID();
    ID.ID();
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);

    ID.ID();
    ID.ID();
    ID.ID();
    ID.ID();
    if (ID.ID)
      ID.ID(STRING_DQ);
    else
      ID.ID(STRING_DQ);

    if (ID.ID == ID.ID) ID();

    if (ID.ID == ID.ID || ID.ID == ID.ID) {
      ID.ID(STRING_DQ, true);
    }

    ID.ID();
    ID.ID(STRING_DQ);
    ID.ID();
  }

  void ID(ID<ID<ID>, ID> ID) {
    for (ID<ID> ID : ID.ID()) {
      COMMENT_LINE_C
      ID.ID(STRING_DQ + ID.ID(ID) + STRING_DQ);

      ID.ID(STRING_DQ + ID.ID(ID), STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ + ID.ID(ID), STRING_DQ, STRING_DQ, STRING_DQ);

      for (ID ID : ID)
        if (ID instanceof ID)COMMENT_LINE_C
                                                   COMMENT_LINE_C
          if (ID.ID(ID.ID()))
          ID.ID(STRING_DQ + ID.ID(ID), ((ID) ID).ID(STRING_DQ), STRING_DQ);
          else
          ID.ID(STRING_DQ + ID.ID(ID), ((ID) ID).ID(null), STRING_DQ);

      ID.ID(STRING_DQ + ID.ID(ID) + STRING_DQ);
      ID.ID();
    }

    ID.ID(STRING_DQ);
    for (ID<ID> ID : ID.ID())
      ID.ID(STRING_DQ + ID.ID(ID) + STRING_DQ);
    ID.ID(STRING_DQ);
    ID.ID();
  }

  void ID(ID ID) {
    COMMENT_LINE_C

    if (ID.ID || ID.ID || ID.ID || ID.ID) {
      if (ID.ID)
        ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID(ID.ID().ID()));
      else
        ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID(ID.ID(STRING_DQ)));

      if (ID.ID)
        ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID(ID.ID().ID()));
      else
        ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

      if (ID.ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID(STRING_DQ));
    }

    if (ID.ID() != ID) if (ID.ID || ID.ID || ID.ID || ID.ID || ID.ID
        || ID.ID || ID.ID) {
          if (ID.ID)
            ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID().ID(STRING_DQ));

          else if (ID.ID)
            ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID().ID(STRING_DQ));

          else
            ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID().ID(STRING_DQ));
        }

    if (ID.ID == ID.ID)
      if (ID.ID || ID.ID || ID.ID || ID.ID) {
        if (ID instanceof ID && ID.ID != null)
          ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID);

        if (ID instanceof ID && ID.ID != ID) {
          COMMENT_LINE_C
          ID.ID(STRING_DQ, ID.ID(STRING_DQ),
              ((ID) ID.ID.ID()).ID.ID(STRING_DQ));
        }

        if (ID instanceof ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

        if (ID instanceof ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

        if (ID.ID != ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID.ID(STRING_DQ));

        if (ID.ID != ID)
          ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID != null ? ID.ID.ID(STRING_DQ) : STRING_DQ);

        if (ID.ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

        if (ID.ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

        if (ID.ID) {
          ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

          ID.ID(STRING_DQ, ID.ID(STRING_DQ), ID.ID(STRING_DQ));
        }

        if (ID.ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

        if (ID.ID.ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);

        if (ID.ID) ID.ID(STRING_DQ, ID.ID(STRING_DQ), STRING_DQ);
      }

    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID);
  }

  void ID() {
    for (ID ID : ID.ID()) {
      ID.ID(STRING_DQ + ID.ID().ID() + STRING_DQ + ID.ID());
      ID(ID.ID);
      ID.ID(STRING_DQ + ID.ID().ID() + STRING_DQ + ID.ID());
      ID.ID();
    }

    COMMENT_LINE_C
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID();
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID();
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID();
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID();
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID();
    ID.ID(STRING_DQ, STRING_DQ);
    for (ID ID : ID.ID())
      if (ID instanceof ID)
        ID.ID(STRING_DQ, STRING_DQ + ID.ID(), STRING_DQ);
    ID.ID();
    ID.ID(STRING_DQ, STRING_DQ);
    for (ID ID : ID.ID())
      ID.ID(STRING_DQ, ID.ID(null), STRING_DQ);

    ID.ID();
    ID.ID(STRING_DQ, STRING_DQ);
    for (int ID = INTEGER; ID < ID.ID().ID; ID++)
      for (int ID = INTEGER; ID < ID.ID().ID; ID++)
        if (ID.ID()[ID] != null && ID.ID()[ID].ID(ID)) ID.ID(STRING_DQ,
            ID.ID().ID(ID).ID(null), ID.ID().ID(ID).ID(null));
    ID.ID(STRING_DQ, STRING_DQ);
    for (int ID = INTEGER; ID < ID.ID().ID; ID++)
      for (int ID = INTEGER; ID < ID.ID().ID; ID++)
        if (ID.ID()[ID] != null && ID.ID()[ID].ID(ID)) ID.ID(STRING_DQ,
            ID.ID().ID(ID).ID(null), ID.ID().ID(ID).ID(null));
    ID.ID();
    if (ID.ID == ID.ID) {
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, ID.ID());
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
      ID.ID();
    }

    COMMENT_LINE_C
    for (ID ID : ID.ID()) {
      ID.ID(STRING_DQ, ID.ID(null), ID.ID(ID.ID()));
      ID.ID(STRING_DQ, ID.ID(null), ID.ID(ID.ID()));

      if (ID instanceof ID) ID.ID(STRING_DQ, ID.ID(null), STRING_DQ);

      if (ID instanceof ID) ID.ID(STRING_DQ, ID.ID(null), STRING_DQ);

      if (ID instanceof ID)
        ID.ID(STRING_DQ, ID.ID(null), STRING_DQ);

      if (ID instanceof ID)
        ID.ID(STRING_DQ, ID.ID(null), STRING_DQ);

      if (ID instanceof ID || ID instanceof ID
          || ID instanceof ID)
        ID.ID(STRING_DQ, ID.ID(null), STRING_DQ);

      if (ID instanceof ID)
        ID.ID(STRING_DQ, ID.ID(null), STRING_DQ);
    }
    for (ID ID : ID.ID()) {
      ID.ID(STRING_DQ + ID.ID().ID() + STRING_DQ + ID.ID());
      if (ID.ID) ID.ID(STRING_DQ, ID.ID(), STRING_DQ);
    }

    ID.ID(STRING_DQ);
    ID.ID();
  }

  void ID(ID ID) {
    ID.ID(ID, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(ID);
    ID.ID();

    ID.ID(ID, STRING_DQ, STRING_DQ, STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ, STRING_DQ);

    COMMENT_LINE_C
    ID((ID) ID.ID().ID());

    if (ID == null)
      ID.ID(STRING_DQ, STRING_DQ);
    else
      ID.ID(STRING_DQ, ID.ID(null));
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ);
    ID.ID(STRING_DQ, STRING_DQ + ID.ID().ID());

    ID.ID(STRING_DQ, ID.ID().ID(STRING_DQ));

    ID.ID(STRING_DQ, ID.ID(ID.ID()));

    ID.ID(STRING_DQ, ID.ID ? STRING_DQ : STRING_DQ);

    COMMENT_LINE_C
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ);
    ID.ID(ID);
    ID.ID();
  }

  void ID(ID ID) {
    if (ID.ID != null)
      ID.ID.ID(STRING_DQ, ID.ID);
    else
      ID.ID.ID(ID.ID());
  }

  COMMENT_BLOCK_C
  void ID(ID ID, boolean ID, ID ID) {
    ID.ID.ID(
        STRING_DQ,
        ID ? STRING_DQ : STRING_DQ, ID ? STRING_DQ : STRING_DQ, ID ? STRING_DQ : STRING_DQ, ID);
  }

  boolean ID(ID ID, boolean ID, boolean ID, boolean ID) throws ID {
    COMMENT_LINE_C
    COMMENT_LINE_C
    COMMENT_LINE_C
    switch (ID.ID) {
    case INTEGER: COMMENT_BLOCK_C
      return true;
    case INTEGER: COMMENT_BLOCK_C
      ID.ID();
      if (ID) ID.ID.ID(STRING_DQ);
      ID.ID.ID(STRING_DQ);
      ID(ID, ID, ID ? ID.ID() : STRING_DQ);
      ID.ID.ID(STRING_DQ);
      break;
    case INTEGER: { COMMENT_BLOCK_C
      ID.ID();
      if (ID) ID.ID.ID(STRING_DQ);
      ID.ID.ID(STRING_DQ);
      ID(ID, ID, ID ? ID.ID() : STRING_DQ);
      ID.ID.ID(STRING_DQ);
    }
      break;
    case INTEGER: {
      ID.ID();
      switch (ID.ID) {
      case ID:
      case ID:
        if (ID) ID.ID.ID(STRING_DQ);
        ID.ID.ID(STRING_DQ);
        ID(ID, ID, ID ? ID.ID() : STRING_DQ);
        ID.ID.ID(STRING_DQ);
        break;

      case ID:
        if (ID) ID.ID.ID(STRING_DQ);
        return false;

      case ID:
        if (ID) ID.ID.ID(STRING_DQ);
        ID.ID();
        ID.ID.ID(STRING_DQ);
        ID(ID, ID, ID ? ID.ID() : STRING_DQ);
        ID.ID.ID(STRING_DQ);
        break;

      case ID:
        throw new ID(STRING_DQ);

      default:
        throw new ID(STRING_DQ);
      }
    }
      break;
    default:
      throw new ID(STRING_DQ);
    }

    return true;
  }

  void ID(ID ID, boolean ID) {
    COMMENT_LINE_C
    ID = INTEGER;
    for (ID ID : ID.ID())
      ID.ID = INTEGER;

    ID(ID.ID);

    for (ID ID : ID.ID())
      for (int ID = INTEGER; ID <= ID.ID; ID++) {
        if (ID)
          ID.ID.ID(STRING_DQ);
        else
          ID.ID.ID(STRING_DQ, ID.ID().ID(), ID.ID(), ID.ID(), ID);
      }
  }

  void ID(ID ID) {
    COMMENT_LINE_C
    ID = INTEGER;
    for (ID ID : ID.ID())
      ID.ID = INTEGER;

    ID(ID.ID);

    for (ID ID : ID.ID())
      for (int ID = INTEGER; ID <= ID.ID; ID++) {
        ID.ID.ID(STRING_DQ, ID.ID(), ID);
      }
  }

  void ID(ID ID) {
    COMMENT_LINE_C
    ID = INTEGER;
    for (ID ID : ID.ID())
      ID.ID = INTEGER;

    ID(ID.ID);

    for (ID ID : ID.ID())
      for (int ID = INTEGER; ID <= ID.ID; ID++) {
        ID.ID.ID(STRING_DQ, ID.ID());
      }
  }

  void ID(ID ID, int ID) {
    if (!ID.ID.ID() || ID.ID) {
      ID.ID.ID(STRING_DQ, ID.ID());
      if (ID != INTEGER) ID.ID.ID(STRING_DQ, ID);
    }
  }

  void ID(ID ID) {
    COMMENT_LINE_C
    ID = INTEGER;
    for (ID ID : ID.ID())
      ID.ID = INTEGER;

    ID(ID.ID);

    for (ID ID : ID.ID())
      for (int ID = INTEGER; ID <= ID.ID; ID++) {
        ID.ID();
        ID.ID.ID(STRING_DQ, ID.ID(), ID, ID.ID().ID(), ID.ID());
      }
  }

  void ID(ID ID, ID ID) {
    ID.ID();
    ID.ID.ID(STRING_DQ, ID.ID(), ID);
    ID(ID, INTEGER);
    ID(ID);
    ID.ID.ID(STRING_DQ);
  }

  void ID(ID ID, ID ID, int ID) {
    ID.ID();
    ID.ID.ID(STRING_DQ, ID.ID(), ID);
    ID(ID, ID);
    ID(ID);
    ID.ID.ID(STRING_DQ);
  }

  void ID(ID ID) {
    ID.ID();
    COMMENT_LINE_C
    COMMENT_LINE_C
    COMMENT_LINE_C

    for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {
      if (ID instanceof ID && !ID.ID) ID.ID.ID(STRING_DQ, ID.ID().ID());
    }
  }

  void ID(ID ID, boolean ID, ID ID) throws ID {
    if (ID instanceof ID) {
      ID(ID, false, ID, false);
      COMMENT_LINE_C
      COMMENT_LINE_C
    } else if (ID instanceof ID) {
      ID(ID, false, ID, false);
      COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C
    } else if (ID instanceof ID) {
      ID ID = (ID) ID;
      ID ID = (ID) ID.ID();
      COMMENT_LINE_C
      COMMENT_LINE_C
      for (ID ID : ID.ID()) {
        if (ID.ID().ID(STRING_DQ)) {
          ID.ID();
          ID(ID);
          ID.ID.ID(STRING_DQ,
              ID ? STRING_DQ : STRING_DQ);
        }
        if (ID.ID().ID(STRING_DQ)) {
          ID.ID();
          ID(ID);
          ID.ID.ID(STRING_DQ,
              ID ? STRING_DQ : STRING_DQ);
        }
      }

      ID(ID, true, ID, !((ID) ID.ID()).ID.ID());
      if (ID.ID) {
        ID.ID();
        ID.ID.ID(STRING_DQ, ((ID) ID.ID()).ID());
        ID(ID);
        ID.ID.ID(STRING_DQ,
            ID ? STRING_DQ : STRING_DQ);
      } else
        ID(ID,
            ID ? (ID) STRING_DQ : (ID) STRING_DQ,
            ID.ID);
      COMMENT_LINE_C
      COMMENT_LINE_C
    }
  }

  void ID(ID ID, ID ID) throws ID {
    if (!ID.ID().ID(STRING_DQ)) return;

    if (ID instanceof ID)
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID(ID, (ID) ID.ID());

    else if (ID instanceof ID)
      ID(ID.ID(), ID);

    else if (ID instanceof ID)
      ID.ID(STRING_DQ);

    else if (ID instanceof ID)
      ID.ID(STRING_DQ);

    else if (ID instanceof ID)
      ID.ID(STRING_DQ);

    else if (ID instanceof ID) {
      COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C

      ID ID = null, ID = null, ID = null, ID = null;

      for (ID = ID.ID().ID(); ID != null; ID = ID.ID()) {

        if (ID instanceof ID || ID instanceof ID
            || ID instanceof ID || ID instanceof ID)
          ID(ID.ID(), ID);
        else if (ID instanceof ID) if (ID.ID) {
          ID = ID;
          ID = ID;
        } else {
          ID.ID();
          ID.ID(ID.ID(STRING_DQ));
          ID.ID(ID.ID(STRING_DQ), false); COMMENT_LINE_C
          ID.ID = true; COMMENT_LINE_C

          if (ID.ID() == null) { COMMENT_LINE_C
            ID(ID);
            ID((ID) (ID.ID()));
          }

          if (ID != null) { COMMENT_LINE_C
            ID(ID, true, ID.ID());
            ID((ID) ID);
            ID = null; COMMENT_LINE_C
          } else COMMENT_LINE_C
            ID(ID, STRING_DQ);

          ID(ID, false, ID.ID());
          ID((ID) ID);

          ID.ID();
          ID.ID();
        }
        ID = ID; COMMENT_LINE_C
      }
    }

    else if (ID instanceof ID)
      ID.ID(STRING_DQ);

    else if (ID instanceof ID)
      throw new ID(STRING_DQ);

    else
      throw new ID(STRING_DQ + ID);
  }

  int ID;

  void ID(ID ID) {
    if (ID instanceof ID) {
      ID ID = ((ID) ID.ID());
      if ((!ID.ID.ID() || ID.ID || ID.ID) && ID.ID > ID.ID)
        ID.ID = ID.ID;
    }

    if (ID instanceof ID) {
      if (ID.ID > ID) ID = ID.ID;
    }

    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID);
  }

  void ID(ID ID) throws ID {
    ID.ID.ID(
        STRING_DQ
            + STRING_DQ + STRING_DQ);
    ID.ID();
    ID.ID();

    ID(ID.ID, ID);

    if (ID.ID().ID(STRING_DQ)) ID.ID.ID(STRING_DQ
        + STRING_DQ
        + STRING_DQ + STRING_DQ + STRING_DQ, STRING_DQ + ID.ID());
    ID.ID();
    ID.ID();
    ID.ID(STRING_DQ + ID.ID());
    ID.ID();
  }

  void ID() throws ID {
    COMMENT_LINE_C
    for (ID ID : ID.ID()) {
      if (ID.ID || ID.ID || !ID.ID.ID()) {
        ID.ID();
        ID.ID.ID(STRING_DQ, ID.ID().ID(), ID.ID());
        ID.ID();
        for (ID ID : ID.ID()) {
          ID.ID();
          ID.ID.ID(STRING_DQ, ID.ID(), ID.ID());
        }
        for (ID ID : ID.ID) {
          ID.ID();
          ID.ID.ID(STRING_DQ, ID.ID(), ID.ID());
        }

        for (ID ID : ID.ID())
          ID.ID = INTEGER;

        ID(ID.ID);

        for (ID ID : ID.ID())
          for (int ID = INTEGER; ID <= ID.ID; ID++) {
            ID.ID();
            ID.ID.ID(STRING_DQ, ID.ID(), ID);
          }

        ID.ID();
        ID.ID.ID(STRING_DQ);
        ID.ID();
        ID.ID.ID(STRING_DQ);
        for (ID ID : ID.ID) {
          ID.ID();
          ID.ID.ID(STRING_DQ, ID.ID(), ID.ID());
        }

        ID.ID();
        ID.ID.ID(STRING_DQ);

        ID.ID();
        ID.ID();
        ID.ID.ID(STRING_DQ);
      }
    }
    COMMENT_LINE_C

    COMMENT_LINE_C
    for (ID ID : ID.ID()) {
      if (ID.ID.ID() && !ID.ID) {
        ID.ID();
        ID.ID.ID(STRING_DQ, STRING_DQ + ID.ID());
      } else {
        ID.ID();
        ID.ID.ID(STRING_DQ, STRING_DQ + ID.ID(),
            ID.ID().ID(), ID.ID(), ID.ID());
      }
      ID(ID, false);
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID(ID);

    }
    COMMENT_LINE_C
    ID.ID();
    ID.ID.ID(
        STRING_DQ);
    ID.ID();
    ID.ID();
    ID.ID.ID(STRING_DQ);
    ID.ID();

    for (ID ID : ID.ID()) {
      if (ID.ID.ID() && !ID.ID) {
        ID.ID();
        ID.ID.ID(STRING_DQ, ID.ID(STRING_DQ));
        ID.ID.ID(STRING_DQ, STRING_DQ + ID.ID());
      } else {
        ID.ID();
        ID.ID.ID(STRING_DQ, ID.ID(STRING_DQ));
        ID.ID.ID(STRING_DQ, STRING_DQ + ID.ID(), ID.ID().ID(),
            ID.ID());
      }
      ID(ID, true);
      ID.ID.ID(STRING_DQ);
    }

    ID.ID();
    ID.ID();
    ID.ID.ID(STRING_DQ);
    ID.ID();
    ID.ID();
    ID.ID.ID(STRING_DQ);

    COMMENT_LINE_C
    if (ID.ID()) {
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID();
      ID.ID.ID(STRING_DQ);

      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID(STRING_DQ);
      return;
    }

    if (ID.ID().ID || ID.ID().ID
        || !ID.ID().ID.ID()) {
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID();
      ID.ID.ID(STRING_DQ, ID.ID().ID().ID(),
          ID.ID().ID(), ID.ID().ID(), ID.ID().ID().ID(),
          ID.ID().ID());
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID(STRING_DQ, ID.ID().ID() + STRING_DQ);
      ID.ID(STRING_DQ);

      ID.ID();
      ID.ID.ID(STRING_DQ, ID.ID().ID().ID(),
          ID.ID().ID(), ID.ID().ID());
      ID.ID();
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID.ID(
          STRING_DQ,
          ID.ID().ID());
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ, ID.ID().ID(),
          INTEGER);
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID(STRING_DQ);
    }

    else {
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID.ID(
          STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID(STRING_DQ, STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ);
      ID.ID(STRING_DQ);
    }
  }
}

public abstract class ID {
  int ID = INTEGER;

  abstract ID ID();

  protected ID ID;

  ID ID() {
    return ID;
  }

  COMMENT_BLOCK_C
  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  COMMENT_BLOCK_C
  void ID() {
  }

  void ID() {
  }

  void ID(int ID) {
    ID = ID;
  }

  void ID() {
    ID++;
  }

  void ID() {
    if (ID > INTEGER) ID--;
  }

  void ID(ID ID) {
  }

  COMMENT_BLOCK_C
  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID() {
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  ID ID() {
    return null;
  }

  void ID(ID ID) {
  }

  void ID() {
  }

  void ID() {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, boolean ID) {
  }

  void ID(ID ID) {
  }

  void ID() {
  }

  void ID(ID ID, boolean ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID() {
  }

  void ID() {
  }

  COMMENT_BLOCK_C
  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  COMMENT_BLOCK_C
  void ID(ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {

  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID) {
  }

  void ID(ID ID, ID ID, ID ID) {
  }

  COMMENT_BLOCK_C
  void ID(ID ID, ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID) {
  }

  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
  }

}




public class ID extends ID {

  public ID(ID ID) {
    super();
    this.ID = ID;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  ID ID;

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  ID ID() {
    return STRING_DQ;
  }

  @ID
  void ID(ID ID, ID ID) {
    if (ID != null) ID.ID(STRING_DQ, ID);

    ID.ID(STRING_DQ + STRING_DQ
        + STRING_DQ + STRING_DQ + STRING_DQ
        + STRING_DQ + STRING_DQ + STRING_DQ
        + STRING_DQ + STRING_DQ + STRING_DQ + STRING_DQ
        + STRING_DQ + STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID.ID();
  }

  @ID
  void ID() {
    ID.ID(STRING_DQ);
  }

  @ID
  void ID() {
    for (int ID = INTEGER; ID < ID; ID++)
      ID.ID(STRING_DQ);
  }

  @ID
  void ID(ID ID) {
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID() {
    ID.ID(STRING_DQ);
  }

  @ID
  void ID() {
    ID.ID(STRING_DQ);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID() {
    ID();
    ID.ID(STRING_DQ);
  }

  @ID
  void ID() {
    ID();
    ID.ID(STRING_DQ);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    if (ID != null) ID.ID(STRING_DQ, ID);
    ID.ID(STRING_DQ);
    ID();
  }

  @ID
  void ID() {
    ID();
    ID.ID(
        STRING_DQ);
    ID();
  }

  @ID
  ID ID() {
    return STRING_DQ;
  };

  @ID
  ID ID() {
    return STRING_DQ;
  };

  @ID
  ID ID() {
    return STRING_DQ;
  };

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
    ID();
  }

  @ID
  void ID(ID ID, boolean ID) {
    if (ID) ID();
    ID();
    ID.ID(STRING_DQ, ID);
    ID();
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ);
    ID();
  }

  @ID
  void ID() {
    ID();
    ID.ID(STRING_DQ);
    ID();
  }

  @ID
  void ID(ID ID, boolean ID) {
    if (ID) ID();
    ID();
    ID();
    ID.ID(STRING_DQ);
  }

  @ID
  void ID(ID ID) {
    ID = ID;
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID = ID;
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID = ID;
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID.ID(STRING_DQ);
  }

  int ID;

  @ID
  void ID(ID ID) {
    ID();
    ID = INTEGER;
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    COMMENT_LINE_C
    ID.ID(STRING_DQ, ID, ID++);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
    ID();
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
    ID();
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID, ID);
    ID();
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
    ID();
  }

  @ID
  void ID(ID ID) {
    ID();
    ID();
    ID.ID(STRING_DQ);
  }

  @ID
  void ID(ID ID) {
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
    ID();
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
    ID();
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID, ID);
    ID();
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID, ID);
    ID();
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
    ID();
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
    ID();
  }

  @ID
  void ID(ID ID) {
    ID();
    ID();
    ID.ID(STRING_DQ);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID, ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
    ID();
    ID.ID(STRING_DQ + ID + STRING_DQ + ID + STRING_DQ);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID, ID, ID);
  }

  @ID
  void ID(ID ID, ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }

  @ID
  void ID(ID ID) {
    ID();
    ID.ID(STRING_DQ, ID);
  }


}

public class ID {
  private final ID ID;
  private final ID ID;
  private final ID ID;
  private boolean ID;
  private boolean ID;
  private final ID ID;
  private ID ID;
  private final ID<ID> ID = new ID<>();
  private final ID<ID> ID = new ID<>();
  private final ID<ID, ID> ID = new ID<>();
  private final ID<ID> ID = new ID<ID>();
  private final ID<ID> ID = new ID<ID>();
  private final ID<ID, ID> ID = new ID<>();
  private final ID<ID, ID> ID = new ID<>();
  private final ID<ID, ID> ID = new ID<>();
  private final ID<ID, ID> ID = new ID<>();
  private final ID<ID, ID> ID = new ID<>();
  private final ID<ID, ID> ID = new ID<>();
  private final ID[] ID;
  private final ID[] ID;

  private final boolean ID = false;
  private boolean ID = true;
  private int ID;
  private int ID;
  private int ID;
  private int ID;
  COMMENT_LINE_C
  private int ID;

  private final ID<ID> ID = new ID<ID>();
  private final ID<ID> ID = new ID<ID>();

  private final ID ID = new ID();
  private final ID ID = new ID();

  private ID ID;

  private ID<ID> ID = new ID<ID>();

  private ID ID;

  private int ID;
  private int ID;
  private int ID;

  ID<ID<ID>, ID> ID = new ID<>();
  private final ID<ID, ID> ID = new ID<>();

  private final ID<ID<ID> > ID = new ID<>();
  private final ID<ID<ID>, ID> ID = new ID<>();
  private int ID = INTEGER;

  public ID(ID ID, ID ID, ID ID, boolean ID, ID ID)
      throws ID, ID {
    this(ID, ID, ID, ID.ID(), ID, null, ID);
  }

  public ID(ID ID, ID ID, ID ID, ID ID) throws ID, ID {
    this(ID, ID, ID, ID.ID(), false, null, ID);
  }

  public ID(ID ID, ID ID, ID ID, ID ID, boolean ID,
      ID<ID, ID<ID> > ID, ID ID) throws ID, ID {
    this.ID = ID;
    this.ID = ID;
    this.ID = ID;

    ID ID;
    ID ID = ID.ID().ID();
    if (ID == null)
      throw new ID(STRING_DQ + ID.ID().ID() + STRING_DQ);
    ID.ID(ID.ID());
    ID.ID(ID.ID());

    COMMENT_LINE_C
    for (ID ID : ID.ID()) {COMMENT_LINE_C
      ID = ID(ID.ID(), ID.ID());
      ID.ID.ID(ID.ID());
    }

    COMMENT_LINE_C
    ID = ID(ID);

    COMMENT_LINE_C
    ID = ID;
    if (ID) {
      ID = ID(ID, STRING_DQ);
      ID ID = new ID(ID++, ID);
      ID ID = new ID(ID++);
      ID ID = (ID) ID.ID(new ID(ID++));
      ID ID = (ID) ID
          .ID(new ID(ID++, ID));
      ID ID = (ID) ID.ID(new ID(ID++));

      ID.ID = ID.ID = ID.ID = ID.ID = ID.ID = ID;
      ID.ID = ID.ID = ID.ID = ID.ID = ID;

      ID.ID = INTEGER;
      ID.ID = ID.ID = INTEGER;

      ID.ID.ID(ID);

      ID = ID;
    }

    COMMENT_LINE_C
    for (ID ID : ID.ID()) {
      COMMENT_LINE_C
      ID = ID(ID);

      for (ID ID : ID.ID()) {
        COMMENT_LINE_C
        ID(ID, ID, ID);
      }
    }

    if (ID.ID()) throw new ID(STRING_DQ);

    COMMENT_LINE_C
    for (ID ID : ID.ID()) {
      ID().ID(ID(ID, ID.ID().ID().ID()));
      ID(ID(ID.ID(), ID.ID().ID().ID()));
    }
    COMMENT_LINE_C
    COMMENT_LINE_C

    COMMENT_LINE_C
    if (!ID.ID && !ID.ID(new ID(STRING_DQ)))
      ID().ID(new ID(STRING_DQ));

    for (ID ID : ID.ID()) {
      if (ID.ID().ID().ID(STRING_DQ))
        ID().ID(new ID(ID.ID().ID().ID()));
      else if (ID.ID().ID().ID(STRING_DQ))
        ID()
            .ID(new ID(ID(ID.ID(), ID.ID().ID().ID())));
      else
        throw new ID(STRING_DQ + ID);
    }

    ID();

    COMMENT_LINE_C
    ID = new ID[ID];
    ID = new ID[ID];
    for (ID ID : ID.ID().ID()) {
      COMMENT_LINE_C
      ID(ID, ID, ID, ID.ID().ID(ID));
    }

    for (ID ID : ID.ID().ID()) {
      COMMENT_LINE_C
      ID(ID, ID, ID, ID.ID().ID(ID));
    }
    COMMENT_LINE_C
  }

  public boolean ID(ID ID, ID ID, ID ID) throws ID {
    boolean ID = ID.ID
        .ID((ID) ID(ID, ID, new ID(INTEGER), ID, false));
    if (ID) ID = true;
    return ID;
  }

  private void ID(ID ID) throws ID {
    if (ID instanceof ID) {
      ID.ID().ID = true;
      ID(ID.ID());
    } else if (ID instanceof ID)
      ID(ID.ID());

    else if (ID instanceof ID)
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID(ID);

    else if (ID instanceof ID)
      ID(ID.ID().ID());

    else if (ID instanceof ID || ID instanceof ID
        || ID instanceof ID || ID instanceof ID || ID instanceof ID)
      ID.ID().ID = true;
    else
      throw new ID(STRING_DQ + ID);
  }

  private void ID(ID ID) throws ID {
    COMMENT_LINE_C

    if (ID instanceof ID) {
      COMMENT_LINE_C
      ID(ID.ID());
    }

    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID);
  }

  private void ID(ID ID, ID ID) {
    if (ID.ID() == ID) ID.ID = ID++;
    if (ID instanceof ID && ID.ID == INTEGER)
      ID.ID = ID++;
    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID, ID);
  }

  COMMENT_LINE_C
  private ID ID(ID ID, ID ID, ID ID,
      ID ID, boolean ID) throws ID {
    COMMENT_LINE_C
    ID ID = ID;
    boolean ID = false;
    boolean ID = true;

    ID = true;
    if (ID(ID, STRING_DQ)) {
      if (ID) {
        ID = true; COMMENT_LINE_C
        ID = ID; COMMENT_LINE_C
      } else
        ID = ID.ID(new ID(ID++));

      COMMENT_BLOCK_C
    } else if (ID(ID, STRING_DQ)) {
      ID = true;
      ID = ID.ID(new ID(ID++));
      if (ID(ID, ID, ID)) return ID; COMMENT_LINE_C
    } else if (ID(ID, STRING_DQ)) {
      ID = true;
      ID = ID.ID(new ID(ID++));
      if (ID(ID, ID, ID)) return ID; COMMENT_LINE_C
    } else if (ID(ID, STRING_DQ)) {
      ID = true;
      ID = ID.ID(new ID(ID++));
      if (ID(ID, ID, ID)) return ID; COMMENT_LINE_C
    } else if (ID(ID, STRING_DQ))
      ID = ID.ID(new ID(ID++));
    else if (ID(ID, STRING_DQ))
      ID = ID.ID(new ID(ID++));
    else if (ID(ID, STRING_DQ))
      ID = ID.ID(new ID(ID++));
    else if (ID(ID, STRING_DQ))
      ID = ID.ID(new ID(ID++));
    else if (ID(ID, STRING_DQ))
      ID = ID.ID(new ID(ID++));
    else if (ID(ID, STRING_DQ)) {
      ID = ID.ID(new ID(ID++));
    } else if (ID(ID, STRING_DQ)) {
      ID = false;
      ID = ID.ID(new ID(ID++));
      ID = ID.ID(new ID(ID++, ((ID) ID.ID().ID()).ID));
    } else { COMMENT_LINE_C
      ID = false;
      if (ID) {
        ID = ID.ID(new ID(ID++));
        ID.ID(new ID(ID++));
      } else { COMMENT_LINE_C
        if (ID(ID, STRING_DQ))
          ID.ID(new ID(ID++,
              ID(ID, ((ID) ID.ID().ID()).ID)));
        else if (ID(ID, STRING_DQ))
          ID = ID.ID(
              new ID(ID++, ID(((ID) ID.ID().ID()).ID)));
        else if (ID(ID, STRING_DQ)) {
          ID ID = ((ID) ID.ID().ID()).ID;
          if (ID.ID(STRING_DQ, STRING_DQ).ID() != ID.ID())
            throw new ID(STRING_DQ + ID.ID() + STRING_DQ + ID + STRING_DQ);
          ID = ID.ID(new ID(ID++, ID(ID)));
        } else if (ID(ID, STRING_DQ)) {
          ID ID = ((ID) ID.ID().ID()).ID;
          if (ID.ID(STRING_DQ, STRING_DQ).ID() != ID.ID())
            throw new ID(STRING_DQ + ID.ID() + STRING_DQ + ID + STRING_DQ);
          ID = ID.ID(new ID(ID++, ID(ID)));
        } else if (ID(ID, STRING_DQ))
          ID = ID.ID(
              new ID(ID++, ID(((ID) ID.ID().ID()).ID)));
        else if (ID(ID, STRING_DQ))
          ID = ID.ID(new ID(ID++, ID));
        else
          throw new ID(STRING_DQ + ID.ID().ID[ID.ID().ID()]
              + STRING_DQ);

        ID(ID, ID, ID);
      }
      if (ID) ID.ID(new ID(ID++));

    }

    if (ID) for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID, ID, ID, ID, ID);

    return ID;
  }

  COMMENT_BLOCK_C
  private void ID(ID ID) throws ID, ID {
    boolean ID = true;
    COMMENT_LINE_C
    int ID = INTEGER;
    ID = new ID(STRING_DQ + ID);
    ID ID = new ID(--ID);
    ID.ID(ID);
    for (ID ID : ID) {
      ID.ID = new ID(--ID, ID);
      ID.ID.ID = true;
      ID.ID.ID(ID);
      ID.ID(ID.ID);
      for (ID ID : ID.ID())
        ID.ID.ID(ID);
    }

    COMMENT_LINE_C
    for (ID ID : ID)
      for (ID ID : ID.ID())
        for (ID ID : ID) { COMMENT_LINE_C
          ID = ID = INTEGER;
          ID(ID, ID);
        }

    if (ID().ID > INTEGER) ID.ID(STRING_DQ);

    COMMENT_LINE_C
    if (ID) {
      for (ID ID : ID)
        ID.ID.ID(ID);
    } else
      ID.ID.ID(ID);

    COMMENT_LINE_C
    while (ID) {
      ID = false;
      ID |= (ID((ID) ID.ID(), INTEGER, null, null));
    }

    COMMENT_LINE_C
    ID = true;
    COMMENT_LINE_C
    while (ID) {
      COMMENT_LINE_C
      COMMENT_LINE_C
      ID = false;
      ID = null;
      ID |= (ID((ID) ID.ID(), null, null, null, INTEGER));
      ID |= (ID((ID) ID.ID()));
    }

    COMMENT_LINE_C
    COMMENT_LINE_C
    ID((ID) ID.ID());
    ID((ID) ID.ID());
    ID((ID) ID.ID());

    COMMENT_LINE_C
    ID = true;
    ID = false;
    COMMENT_LINE_C
    for (ID ID : ID)
      for (ID ID : ID.ID()) {
        COMMENT_BLOCK_C
        COMMENT_LINE_C
        COMMENT_LINE_C
        COMMENT_LINE_C
        if (ID.ID().ID() instanceof ID) {
          if (ID.ID().ID().ID().ID() != null) ID(ID.ID());
          COMMENT_LINE_C
        } else
          ID(ID.ID());

      }
    COMMENT_LINE_C

    COMMENT_LINE_C
    ID = INTEGER;

    ID(ID);

    COMMENT_LINE_C

    COMMENT_LINE_C
    ID();

    if (ID().ID > INTEGER) ID.ID(STRING_DQ);

    ID = false;
  }

  public void ID() throws ID, ID {
    boolean ID = false;
    if (ID) {
      for (ID ID : ID)
        if (ID.ID().ID()) {
          ID = true;
          throw new ID(STRING_DQ + ID.ID() + STRING_DQ);
        }

      if (ID) throw new ID(STRING_DQ);
      ID();
      ID(ID);
    }
  }

  private boolean ID(ID ID, ID ID, ID ID,
      ID ID, int ID) {
    if (ID == null) return false;
    COMMENT_LINE_C
    COMMENT_LINE_C

    boolean ID = false;

    if (ID instanceof ID) ID = (ID) ID;
    if (ID == INTEGER) ID = (ID) ID;

    if (!(ID instanceof ID)) COMMENT_LINE_C
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID |= (ID(ID, ID, ID, ID, ID + INTEGER));
    else
      ID.ID = ID.ID() != null;

    COMMENT_LINE_C
    COMMENT_LINE_C
    COMMENT_LINE_C

    COMMENT_LINE_C
    if (ID.ID() == null && ID != null) {
      ID |= (ID.ID() != ID);
      ID.ID = ID;
    }

    ID |= (ID.ID() != ID);
    ID.ID = ID;

    if (ID.ID == null) {
      ID |= (ID.ID != ID);
      ID.ID = ID;
    }
    if (ID.ID == null) {
      ID |= (ID.ID != ID);
      ID.ID = ID;
    } COMMENT_LINE_C
    COMMENT_LINE_C
    if (ID.ID == null) {
      ID |= (ID.ID != ID);
      ID.ID = ID;
    }

    if (ID.ID == null) {
      ID |= (ID.ID != ID);
      COMMENT_LINE_C
      COMMENT_LINE_C
    }
    if (ID.ID == null) {
      ID |= (ID.ID != ID);
      ID.ID = ID;
    }
    if (ID.ID == null) {
      ID |= (ID.ID != ID);
      ID.ID = ID;
    }

    COMMENT_LINE_C
    COMMENT_LINE_C
    ID |= (ID.ID != (ID instanceof ID && ID == INTEGER && ID.ID() == null));
    ID.ID = (ID instanceof ID && ID == INTEGER && ID.ID() == null);

    COMMENT_LINE_C
    if (ID instanceof ID || ID instanceof ID) {
      ID |= (ID(ID).ID != true);
      ID(ID).ID = true;
    }

    if (ID.ID.ID) {
      ID |= (ID.ID != true);
      ID.ID = true;
    }

    COMMENT_LINE_C
    if (ID instanceof ID) {
      ID |= (ID(ID).ID != true);
      ID(ID).ID = true;
    }

    COMMENT_LINE_C
    if (ID instanceof ID) {
      ID |= (ID(ID).ID != true);
      ID(ID).ID = true;
    }

    COMMENT_LINE_C
    if (ID instanceof ID) {
      ID |= (ID(ID).ID != true);
      ID(ID).ID = true;
    }

    COMMENT_LINE_C
    if (ID instanceof ID) {
      ID |= (ID(ID).ID != true);
      ID(ID).ID = true;
    }

    COMMENT_LINE_C

    COMMENT_LINE_C
    if (ID instanceof ID) {
      ID |= (ID.ID().ID != ID);
      ID.ID().ID = ID;
    }

    COMMENT_LINE_C
    if (ID instanceof ID && ID != null) {
      if (ID.ID) {
        ID |= (ID.ID != ID);
        ID.ID = ID;
      } else {
        ID |= (ID.ID != ID);
        ID.ID = ID;
      }
      ID = null;
    }

    if (ID instanceof ID) ID = ID;

    COMMENT_LINE_C
    if (ID instanceof ID) {
      ID ID = ID(ID);
      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {
        ID ID = ID(ID);

        ID |= (ID.ID != ID);
        ID.ID = ID;
        COMMENT_LINE_C
        ID |= (ID.ID != true);
        ID.ID = true;
      }
    }

    COMMENT_LINE_C

    COMMENT_LINE_C
    if (ID instanceof ID && ID.ID() == null) {
      ID |= (ID.ID != true);
      ID.ID = true;
    }

    if (ID.ID() != null && ID.ID().ID
        && (ID instanceof ID || ID instanceof ID || ID instanceof ID)) {
      ID |= (ID.ID != true);
      ID.ID = true;
    }

    COMMENT_LINE_C
    if (ID instanceof ID) if (ID.ID() != ID && ID.ID() != null) {
      ID |= (ID.ID != true);
      ID.ID = true;
    }

    COMMENT_LINE_C
    if (ID instanceof ID || (ID instanceof ID && ID == INTEGER))
      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) {
        ID |= (ID.ID != ID(ID));
        ID.ID = ID(ID);
      }
    else if (ID instanceof ID) if (ID.ID() != null) {
      ID |= (ID.ID().ID != ID);
      ID.ID().ID = ID;
    } else if (ID instanceof ID || ID instanceof ID) {
      ID |= (ID.ID != ID(ID));
      ID.ID = ID(ID);
    }

    COMMENT_LINE_C
    if (ID instanceof ID || ID instanceof ID || ID instanceof ID
        || ID instanceof ID || ID instanceof ID
        || ID instanceof ID || ID instanceof ID) {
      ID |= (ID.ID != ID.ID());
      ID.ID = ID.ID();
    }
    if (ID instanceof ID || ID instanceof ID) {
      ID |= (ID.ID != ID(ID));
      ID.ID = ID(ID);
    }

    COMMENT_LINE_C

    return ID;
  }

  private void ID(ID ID) throws ID {
    COMMENT_LINE_C

    if (ID instanceof ID)
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID(ID);
    else if (ID instanceof ID)
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID(ID);
    else if (ID instanceof ID)
      ID(ID.ID());
    else if (ID instanceof ID || ID instanceof ID || ID instanceof ID)
      ;
    else if (ID instanceof ID)
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID(ID);

    else if (ID instanceof ID && ID.ID().ID().ID().ID() == null) COMMENT_LINE_C
    {
      ID ID = ID.ID().ID();
      if (!(ID instanceof ID || ID instanceof ID)) ID(ID);
    }

    else if (ID instanceof ID)
      ID(ID.ID().ID());

    else if (ID instanceof ID) {
      COMMENT_LINE_C
      boolean ID = false;

      if (ID.ID().ID() != null) {
        ID ID = ID.ID().ID();
        if (ID instanceof ID && ID.ID() == ID.ID()) ID = true;
      }
      COMMENT_LINE_C

      if (!(ID && ID.ID.ID(ID))) ID.ID().ID = true;
    }

    else if (ID instanceof ID)
      ID.ID().ID = true;

    else if (ID instanceof ID || ID instanceof ID || ID instanceof ID
        || ID instanceof ID)
      ;

    else
      throw new ID(STRING_DQ + ID.ID() + STRING_DQ);

  }

  private void ID(ID ID) {
    COMMENT_LINE_C
    if (ID == null) return;

    if (ID instanceof ID || ID instanceof ID
        || ID instanceof ID || ID instanceof ID || ID instanceof ID
        || (ID instanceof ID) && !(ID.ID || ID.ID)) {
      COMMENT_LINE_C
      ID = false;
    }

    if (ID instanceof ID || ID instanceof ID
        || ID instanceof ID || ID instanceof ID
        || ID instanceof ID || ID instanceof ID) {
      COMMENT_LINE_C
      ID = true;
    }

    ID(ID.ID());
    ID(ID.ID());
  }

  private void ID(ID ID) {
    ID(ID.ID);
    ID(ID.ID);
    ID(ID.ID());

    for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID);
  }

  private boolean ID(ID ID) {
    if (ID == null) return false;

    boolean ID = false;

    if (!(ID instanceof ID)) COMMENT_LINE_C
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
      ID(ID);

    ID |= (ID.ID != ID(ID));
    ID.ID = ID(ID);
    COMMENT_LINE_C
    return ID;
  }

  private void ID(ID ID) {
    ID.ID = true;
    if (ID.ID() != null) ID.ID().ID = true;

    if (ID instanceof ID || ID instanceof ID || ID instanceof ID
        || ID instanceof ID) COMMENT_LINE_C
      COMMENT_LINE_C
      COMMENT_LINE_C
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID(ID);
    else if (ID instanceof ID) { COMMENT_LINE_C
      ID ID = null;

      for (ID ID = ID.ID(); ID != null; ID = ID.ID()) { COMMENT_LINE_C
        if (!(ID instanceof ID)) ID = ID;
      }

      if (ID instanceof ID)
        ID(ID);
      else
        ID.ID().ID = true;
    }

  }

  private boolean ID(ID ID, int ID, ID ID, ID ID) throws ID {
    boolean ID = false;

    if (ID == null) return ID;

    COMMENT_LINE_C

    ID |= (ID(ID.ID(), ID, ID, ID));

    ID ID = ID;

    if (ID instanceof ID || ID instanceof ID || ID instanceof ID
        || ID instanceof ID)
      ID = ID;

    if (ID instanceof ID) {
      ; COMMENT_LINE_C
    }

    else if (ID instanceof ID) {
      ID |= ID.ID.ID(((ID) ID.ID()).ID());
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID |= ID.ID.ID(ID.ID);

      ID |= (((ID) ID.ID()).ID()).ID(ID.ID);
    }

    else if (ID instanceof ID) {
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID |= ID.ID.ID(ID.ID);
    }

    else if (ID instanceof ID) {
      COMMENT_LINE_C
      for (ID ID = ID.ID().ID(); ID != null; ID = ID.ID().ID()) {
        ID<ID> ID = new ID<ID>(ID.ID);
        if (ID.ID().ID() != null) ID.ID(ID);

        ID |= ID.ID.ID(ID);
        if (!ID.ID.ID(ID)) break;
      }
    }

    else if (ID instanceof ID) {
      if (ID.ID() == null) { COMMENT_LINE_C
        if (ID == null)
          ID |= ID.ID.ID(ID);
        else {
          ID |= ID.ID.ID(ID.ID().ID); COMMENT_LINE_C
          if (ID instanceof ID || ID instanceof ID)
            ID |= ID.ID.ID(ID.ID);
        }
      } else { COMMENT_LINE_C
        ID<ID> ID = new ID<ID>(ID.ID().ID);
        ID.ID(ID);

        ID |= ID.ID.ID(ID);

        if (ID.ID().ID.ID(ID)) ID |= ID.ID.ID(ID.ID().ID().ID); COMMENT_LINE_C
      }

      COMMENT_LINE_C
      ID<ID> ID = new ID<ID>(ID.ID);
      if (ID.ID(ID)) if (ID == null)
        ID.ID(ID.ID);
      else {
        ID.ID(ID.ID().ID());
        COMMENT_LINE_C
        if (ID instanceof ID || ID instanceof ID)
          ID.ID(ID.ID());
      }

      ID.ID(ID);
      ID |= ID.ID().ID(ID);

      return ID; COMMENT_LINE_C
    }

    else if (ID instanceof ID) {
      ID ID = (ID) ID.ID();

      ID |= ID.ID.ID(ID.ID);

      ID<ID> ID = new ID<ID>();
      ID.ID(ID.ID().ID);
      ID.ID(ID);

      ID |= ID.ID.ID(ID);

      if (ID.ID().ID.ID(ID)) COMMENT_LINE_C
        ID |= ID.ID.ID(ID.ID);

      ID |= ID.ID.ID(ID.ID);
    }

    else if (ID instanceof ID)
      ; COMMENT_LINE_C

    else if (ID instanceof ID)
      ; COMMENT_LINE_C

    else if (ID instanceof ID || ID instanceof ID || ID instanceof ID
        || ID instanceof ID) {
      ID |= ID.ID.ID(ID.ID().ID);
      if (ID instanceof ID || ID instanceof ID) ID |= ID.ID.ID(ID);
      if (!(ID instanceof ID)) {COMMENT_LINE_C
        ID<ID> ID = new ID<ID>(ID.ID().ID);
        if (ID.ID(ID)) ID.ID(ID.ID().ID());

        ID.ID(ID);
        ID |= ID.ID().ID().ID(ID);

      }
    }

    else
      throw new ID(STRING_DQ + ID.ID().ID() + STRING_DQ);

    ID |= (ID(ID.ID(), ID + INTEGER, ID == INTEGER ? (ID) ID.ID() : ID,
        ID.ID(ID)));
    return ID;
  }

  private void ID() {
    COMMENT_LINE_C
    COMMENT_LINE_C
    for (ID ID : ID) { COMMENT_LINE_C
      for (ID ID : ID.ID()) { COMMENT_LINE_C
        int ID = INTEGER;
        ID<ID> ID = new ID<>();
        COMMENT_LINE_C
        for (ID ID = ID.ID(); ID != null; ID = ID.ID()) { COMMENT_LINE_C
          COMMENT_LINE_C
          if (ID.ID() == null)
            ID.ID = ID;
          else if (ID instanceof ID) {
            ID.ID = ID;
            COMMENT_LINE_C
            if (ID.ID(ID) == null) ID.ID(new ID<>(ID), (ID) ID);
            ID().ID((ID) ID, ID.ID(ID));
            COMMENT_LINE_C
          } else {
            ID.ID = ++ID;
            if (!(ID instanceof ID)) ID.ID(ID.ID());
          }
        }
      }
    }
    COMMENT_LINE_C
  }

  private ID ID(ID ID, ID ID) throws ID {

    int ID = ID(ID, ID);
    COMMENT_LINE_C
    return ID;
  }

  private ID ID(ID ID, ID ID) throws ID {
    if (((ID) ID.ID()).ID().ID(STRING_DQ)) {
      ID ID = ID().ID(new ID(ID, ID(ID)));
      if (ID == null) ID = ID().ID(ID(ID));
      return (ID == null) ? INTEGER : ID.ID();
    }
    if (((ID) ID.ID()).ID().ID(STRING_DQ)) {
      ID ID = ID.ID(ID(ID));
      return (ID == null) ? INTEGER : ID.ID();
    }
    if (((ID) ID.ID()).ID().ID(STRING_DQ)) {
      ID ID = ID.ID(ID(ID));
      return (ID == null) ? INTEGER : ID.ID();
    }
    if (((ID) ID.ID()).ID().ID(STRING_DQ)) {
      ID ID = ID.ID(ID(ID));
      return (ID == null) ? INTEGER : ID.ID();
    }
    throw new ID(STRING_DQ + ID.ID() + STRING_DQ);
  }

  @ID
  public boolean ID(ID ID) {
    if (this == ID) return true;
    if (ID == null) return false;
    if (ID() != ID.ID()) return false;
    ID ID = (ID) ID;
    if (ID == null) {
      if (ID.ID != null) return false;
    } else if (!ID.ID(ID.ID)) return false;
    return true;
  }

  COMMENT_BLOCK_C
  private void ID(ID[] ID, ID ID, ID ID, ID<ID> ID) throws ID {
    if (ID.ID().ID().ID(STRING_DQ)) COMMENT_LINE_C
      for (ID ID : ID) {
        if (ID instanceof ID) ID(ID, ID, ID.ID(), ID);
      }
    else
      ID(ID, ID, ID(ID, ID), ID);

  }

  private void ID(ID[] ID, ID ID, int ID, ID<ID> ID) throws ID {
    if (ID == INTEGER) return; COMMENT_LINE_C
    for (ID ID : ID)
      if (ID.ID().ID().ID(STRING_DQ))
        for (ID ID : ID) {
          if (ID instanceof ID) ID(ID, ID, ID.ID());
        }
      else
        ID(ID, ID, ID(ID, ID));
  }

  private void ID(ID[] ID, int ID, int ID) {
    COMMENT_LINE_C
    if (ID == INTEGER) return;
    if (ID == INTEGER) return;
    if (ID[ID] == null) ID[ID] = new ID();
    ID[ID].ID(ID);
  }

  public ID ID(ID ID, ID ID) {
    return ID(new ID(ID, ID));
  }

  public ID ID(ID ID) {
    return ID(ID.ID(), ID.ID());
  }

  public ID ID(ID ID) {
    ID ID = ID().ID(ID);
    if (ID == null) {
      ID().ID(ID, ID = new ID(ID)); COMMENT_LINE_C
                                                                                     COMMENT_LINE_C
      COMMENT_LINE_C
      ID.ID(ID);
      ID = true;
    }
    return ID;
  }

  public ID ID(ID ID) throws ID {
    ID ID = ID.ID(ID);
    if (ID == null) {
      ID.ID(ID, ID = new ID(ID)); COMMENT_LINE_C
                                                                                        COMMENT_LINE_C
      COMMENT_LINE_C
      ID.ID(ID);
      ID = true;
    }
    return ID;
  }

  public ID ID(ID ID) {
    ID ID = ID.ID(ID);
    if (ID == null) {
      ID.ID(ID, ID = new ID(ID)); COMMENT_LINE_C
                                                                                                        COMMENT_LINE_C
      COMMENT_LINE_C
      ID.ID(ID);
      ID = true;
    }
    return ID;
  }

  public ID ID(ID ID) {
    ID ID = ID.ID(ID);
    if (ID == null) {
      ID.ID(ID, ID = new ID(ID)); COMMENT_LINE_C
                                                                                                    COMMENT_LINE_C
      COMMENT_LINE_C
      ID.ID(ID);
      ID = true;
    }
    return ID;
  }

  public ID ID(ID ID) {
    ID ID = ID.ID(ID);
    if (ID == null) {
      ID.ID(ID, ID = new ID(ID)); COMMENT_LINE_C
                                                                                            COMMENT_LINE_C
      COMMENT_LINE_C
      ID.ID(ID);
      ID = true;
    }
    return ID;
  }

  public ID ID(ID ID, ID ID) {
    return STRING_DQ + ID + STRING_DQ + ID;
  }

  public ID ID(ID ID) {
    ID ID = ID().ID(ID);
    if (ID == null) {
      ID().ID(ID, ID = new ID(ID));
      ID.ID(ID);
      ID = true;
    }
    return ID;
  }

  public ID ID() {
    return ID;
  }

  public ID ID(int ID) {
    ;
    return ID.ID(ID);
  }

  public ID<ID, ID> ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }

  public int ID() {
    return ID;
  }

  public int ID() {
    return ID;
  }

  public ID ID() {
    return ID && !ID ? ID.ID : ID ? ID.ID : ID.ID;
  }

  public ID[] ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }

  public int ID() {
    return ID;
  }

  public int ID() {
    return ID;
  }

  public ID[] ID() {
    return ID;
  }

  public ID<ID<ID>, ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID, ID> ID() {
    return ID;
  }

  public ID<ID<ID>, ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }

  @ID
  public int ID() {
    final int ID = INTEGER;
    int ID = INTEGER;
    ID = ID * ID + ((ID == null) ? INTEGER : ID.ID());
    return ID;
  }

  public boolean ID() {
    return ID;
  }

  public boolean ID() {
    return ID;
  }

  public boolean ID(int ID) {
    return ID.ID(ID) instanceof ID;
  }

  public boolean ID(int ID) {
    return ID.ID(ID) instanceof ID;
  }

  public boolean ID() {
    return ID;
  }

  public boolean ID(int ID) {
    return ID.ID(ID) instanceof ID;
  }

  public boolean ID(int ID) {
    return ID.ID(ID) == null;
  }

  public boolean ID(int ID) {
    return ID.ID(ID) instanceof ID;
  }

  COMMENT_LINE_C
  public ID ID(ID ID) {
    ID ID;

    if (ID.ID() == null || ID instanceof ID) COMMENT_LINE_C
      ID = ID;
    else
      ID = ID(ID.ID());

    return ID;
  }

  private void ID(ID<ID> ID) {
    if (ID != null && !ID.ID(ID)) {
      COMMENT_LINE_C
      ID.ID(ID, ID++);
    }
  }

  private void ID() {
    ID().ID();
    int ID = INTEGER;

    ID.ID(ID++);
    ID().ID(ID.ID(), ID);
    ID = ID;
    for (ID ID : ID) {
      ID.ID(ID++);
      ID().ID(ID.ID(), ID);
    }
    ID = ID - INTEGER;
    ID.ID(ID++);
    ID().ID(ID.ID(), ID);

    COMMENT_LINE_C
    for (ID ID : ID()) {
      ID.ID(ID++);
      ID().ID(ID.ID(), ID);
    }

    ID = ID;
    for (ID ID : ID) {
      ID.ID(ID++);
      ID().ID(ID.ID(), ID);
    }
    ID = ID - INTEGER;

    COMMENT_LINE_C
    ID = ID;
    COMMENT_LINE_C
  }

  private ID ID(ID ID) {
    COMMENT_LINE_C
    COMMENT_LINE_C
    if (ID.ID && !(ID.ID || ID.ID || ID.ID || ID.ID)) if (ID.ID == ID)
      return ID;
    else
      return ID(ID.ID);
    if (ID.ID) return ID(ID.ID);
    return ID;
  }

  public void ID() {
    ID ID = new ID();

    boolean ID = false;
    for (ID ID : ID)
      ID |= ID instanceof ID;

    if (ID) {
      ID.ID(STRING_DQ);

      for (ID ID : ID)
        if (ID instanceof ID) ID.ID(STRING_DQ + ID.ID());
      ID.ID(STRING_DQ);
    }

    boolean ID = false;
    for (int ID = INTEGER; ID < ID.ID; ID++) {
      ID |= ID[ID] != null && ID[ID].ID();
      ID |= ID[ID] != null && ID[ID].ID();
    }

    if (ID) {
      ID.ID(STRING_DQ);
      for (int ID = INTEGER; ID < ID.ID; ID++)
        for (int ID = INTEGER; ID < ID.ID; ID++)
          if (ID[ID].ID(ID)) ID.ID(ID(ID) + STRING_DQ + ID(ID));

      for (int ID = INTEGER; ID < ID.ID; ID++)
        for (int ID = INTEGER; ID < ID.ID; ID++)
          if (ID[ID].ID(ID)) ID.ID(ID(ID) + STRING_DQ + ID(ID));

      ID.ID();
    }

    ID.ID(STRING_DQ + ID);

    for (ID ID : ID)
      COMMENT_LINE_C
      if (ID.ID != null) ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID.ID(null, STRING_DQ, STRING_DQ, false));
  }

  private boolean ID(ID ID, ID ID, ID ID) throws ID {
    COMMENT_LINE_C
    COMMENT_LINE_C
    ID ID = ID.ID();

    if (ID(ID, STRING_DQ)) return false;

    ID = ID.ID(new ID(ID++));
    ID.ID(new ID(ID++));
    if (ID(ID, STRING_DQ))
      ID.ID(
          new ID(ID++, ID(ID, ((ID) ID.ID().ID()).ID)));
    else if (ID(ID, STRING_DQ))
      ID.ID(
          new ID(ID++, ID(((ID) ID.ID().ID()).ID)));
    else if (ID(ID, STRING_DQ))
      ID.ID(new ID(ID++,
          ID(((ID) ID.ID().ID()).ID)));
    else if (ID(ID, STRING_DQ))
      ID.ID(new ID(ID++,
          ID(((ID) ID.ID().ID()).ID)));
    else if (ID(ID, STRING_DQ))
      ID.ID(
          new ID(ID++, ID(((ID) ID.ID().ID()).ID)));
    else if (ID(ID, STRING_DQ))
      ID.ID(new ID(ID++, ID));
    else
      throw new ID(STRING_DQ + ID.ID());
    ID.ID(new ID(ID++));
    return true;
  }

  private void ID(ID ID, ID ID, ID ID) throws ID {
    if (ID.ID() == null) return;
    COMMENT_LINE_C
    if (!ID(ID.ID(), STRING_DQ)) throw new ID(STRING_DQ);
    for (ID ID = ID.ID().ID(); ID != null; ID = ID.ID())
      if (ID(ID, STRING_DQ)) {
        ID.ID = ((ID) ID.ID().ID().ID).ID;
      } else if (ID(ID, STRING_DQ)) {
        ID.ID = true;
        if (!(ID.ID() instanceof ID)) throw new ID(STRING_DQ + ID + STRING_DQ);
        ((ID) ID.ID()).ID = true;
        ID.ID = true;
      } else if (ID(ID, STRING_DQ)) {
        if (ID.ID != null) throw new ID(STRING_DQ + ID);
        ID.ID = ((ID) ID.ID().ID().ID).ID;
      } else if (ID(ID, STRING_DQ)) {
        ID ID = ID.ID();
        if (ID.ID != ID.ID)
          ID.ID.ID(STRING_DQ + ID + STRING_DQ + ID + STRING_DQ + ID);
        if (ID(ID, STRING_DQ))
          ID.ID = ID.ID;
        else if (ID(ID, STRING_DQ))
          ID.ID = ID.ID;
        else if (ID(ID, STRING_DQ))
          ID.ID = ID.ID;
        else if (ID(ID, STRING_DQ))
          ID.ID = ID.ID;
        else
          throw new ID(STRING_DQ + ID.ID() + STRING_DQ + ID + STRING_DQ + ID);
      }
  }

  private ID ID(ID ID) {
    if (ID == null) return null;
    COMMENT_LINE_C
    COMMENT_LINE_C
    ID ID = null;

    if (ID.ID() == null || ID instanceof ID) COMMENT_LINE_C
      ID = ID;
    else
      for (ID ID = ID.ID(); ID != null; ID = ID.ID())
        ID = ID(ID);

    return ID;
  }

  public void ID(ID<ID> ID) {
    this.ID = ID;
  }

  private ID ID(ID ID) {
    return ID.ID().ID().ID();
  }

  @ID
  public ID ID() {
    ID ID = new ID();
    ID.ID(STRING_DQ);
    ID.ID(STRING_DQ + ID + STRING_DQ);
    ID.ID(ID);
    ID.ID(STRING_DQ);
    ID.ID(ID);
    ID.ID(STRING_DQ);
    ID.ID(ID);
    ID.ID(STRING_DQ);
    ID.ID(ID);
    ID.ID(STRING_DQ);
    ID.ID(ID);
    ID.ID(STRING_DQ);
    ID.ID(ID);
    ID.ID(STRING_DQ);
    ID.ID(ID);
    ID.ID(STRING_DQ);
    ID.ID(ID());
    ID.ID(STRING_DQ);
    ID.ID(ID.ID(ID));
    ID.ID(STRING_DQ);
    ID.ID(ID.ID(ID));
    return ID.ID();
  }

  private boolean ID(ID ID, ID ID) {
    COMMENT_LINE_C
    return ID.ID().ID[ID.ID().ID()].ID(ID);
  }

  public ID ID() {
    return ID;
  }

  public ID<ID, ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID, ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }
}


public abstract class ID implements ID<ID> {
  private int ID;

  public int ID() {
    return ID;
  }

  public void ID(int ID) {
    this.ID = ID;
  }

  abstract public ID ID();

  abstract public ID ID(ID ID);

  COMMENT_LINE_C
  COMMENT_LINE_C
  final public int ID() {
    return this instanceof ID ? INTEGER
        : this instanceof ID ? INTEGER
            : this instanceof ID ? INTEGER
                : this instanceof ID ? INTEGER
                    : this instanceof ID ? INTEGER
                        : this instanceof ID ? INTEGER
                            : this instanceof ID ? INTEGER
                                : this instanceof ID ? INTEGER : this instanceof ID ? INTEGER : INTEGER;

  }

  COMMENT_LINE_C
  COMMENT_LINE_C
  COMMENT_LINE_C
  @ID
  public int ID() {
    final int ID = INTEGER;
    return ID * ID();
  }

  COMMENT_LINE_C
  COMMENT_LINE_C
  COMMENT_LINE_C
  @ID
  public boolean ID(ID ID) {
    if (this == ID) return true;
    if (ID == null) return false;
    if (ID() != ID.ID()) return false;
    ID ID = (ID) ID;
    if (ID() != ID.ID()) return false;
    return true;
  }

  @ID
  public int ID(ID ID) {
    COMMENT_LINE_C

    int ID = this.ID();
    int ID = ID.ID();

    if (ID > ID) return INTEGER;
    if (ID < ID) return -INTEGER;

    return INTEGER;
  }

}


COMMENT_LINE_C
public class ID extends ID {
  private final ID ID;

  public ID(ID ID) {
    this.ID = ID;
  }

  @ID
  public ID ID() {
    return ID;
  }

  @ID
  public ID ID(ID ID) {
    return ID;
  }

  @ID
  public ID ID() {
    return ID;
  }

}


public class ID extends ID {
  @ID
  public int ID() {
    final int ID = INTEGER;
    int ID = super.ID();
    ID = ID * ID + ((ID() == null) ? INTEGER : ID().ID());
    ID = ID * ID + ((ID() == null) ? INTEGER : ID().ID());
    return ID;
  }

  @ID
  public boolean ID(ID ID) {
    if (this == ID) return true;
    if (!super.ID(ID)) return false;
    if (ID() != ID.ID()) return false;
    ID ID = (ID) ID;
    if (ID() == null) {
      if (ID.ID() != null) return false;
    } else if (!ID().ID(ID.ID())) return false;
    if (ID() == null) {
      if (ID.ID() != null) return false;
    } else if (!ID().ID(ID.ID())) return false;
    return true;
  }

  private final ID ID;
  private final ID ID;

  public ID(ID ID, ID ID) {
    super();
    this.ID = ID;
    this.ID = ID;
  }

  @ID
  public ID ID() {
    return ID() + STRING_DQ + ID();
  }

  @ID
  public ID ID() {
    return ID();
  }

  @ID
  public ID ID(ID ID) {
    return STRING_DQ + ID + STRING_DQ + ID();
  }

  public ID ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }
}


public class ID extends ID {
  public ID() {
    super(STRING_DQ);
  }

  @ID
  public ID ID() {
    return STRING_DQ;
  }

  @ID
  public ID ID() {
    return STRING_DQ;
  }

  @ID
  public ID ID(ID ID) {
    return STRING_DQ + ID + STRING_DQ + ID();
  }

  @ID
  public int ID() {
    final int ID = INTEGER * ID();
    int ID = INTEGER;
    ID = ID * ID + ((ID() == null) ? INTEGER : ID().ID());
    return ID;
  }

  @ID
  public boolean ID(ID ID) {
    if (this == ID) return true;
    if (ID == null) return false;
    if (ID() != ID.ID()) return false;
    return true;
  }

  @ID
  public int ID(ID ID) {
    COMMENT_LINE_C

    int ID = this.ID();
    int ID = ID.ID();

    if (ID > ID) return INTEGER;
    if (ID < ID) return -INTEGER;

    COMMENT_LINE_C
    return INTEGER;
  }
}


public class ID extends ID {

  @ID
  public ID ID() {
    return STRING_DQ;
  }

  @ID
  public ID ID() {
    return STRING_DQ;
  }

  @ID
  public ID ID(ID ID) {
    return STRING_DQ + ID + STRING_DQ + ID();
  }

  @ID
  public int ID() {
    final int ID = INTEGER * ID();
    int ID = INTEGER;
    ID = ID * ID;
    return ID;
  }

  @ID
  public boolean ID(ID ID) {
    if (this == ID) return true;
    if (ID == null) return false;
    if (ID() != ID.ID()) return false;
    return true;
  }

  @ID
  public int ID(ID ID) {
    COMMENT_LINE_C

    int ID = this.ID();
    int ID = ID.ID();

    if (ID > ID) return INTEGER;
    if (ID < ID) return -INTEGER;

    COMMENT_LINE_C
    return INTEGER;
  }
}





public class ID extends ID {
  private final ID ID;
  private final ID ID;

  COMMENT_LINE_C
  private final ID<ID> ID = new ID<ID>();
  private final ID<ID> ID = new ID<ID>();
  private final ID<ID> ID = new ID<ID>();

  public ID(ID ID, ID ID) {
    super();
    this.ID = ID;
    this.ID = ID;
  }

  public void ID(ID ID) {
    COMMENT_LINE_C
    ID.ID(ID);
  }

  public void ID(ID ID, ID ID) {
    COMMENT_LINE_C
    ID.ID(new ID(ID, ID));
  }

  public void ID(ID ID) {
    ID.ID(ID);
  }

  @ID
  public ID ID() {
    return ID.ID() + STRING_DQ + ID;
  }

  public void ID() {
    for (ID ID : ID) {
      ID.ID.ID(ID + STRING_DQ);
      ID.ID();
      ID.ID.ID(STRING_DQ);
    }
  }

  public ID ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  @ID
  public ID ID() {
    return STRING_DQ + ID.ID() + STRING_DQ + ID;
  }

  @ID
  public ID ID(ID ID) {
    return STRING_DQ + STRING_DQ + ID.ID() + STRING_DQ + ID;
  }
}







public class ID extends ID {
  protected ID ID;
  protected ID ID;

  protected ID<ID> ID = new ID<ID>();
  protected ID<ID> ID = new ID<ID>();

  protected ID<ID> ID = new ID<ID>();
  protected ID<ID> ID = new ID<ID>();

  protected boolean ID = false;
  protected boolean ID = false;
  protected boolean ID = false;
  protected boolean ID = false;
  protected boolean ID = false;
  protected boolean ID = false;
  public ID ID; COMMENT_LINE_C

  public int ID;

  @ID
  public ID ID() {
    return ID.ID() + STRING_DQ + ID;
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }

  public ID(ID ID, ID ID) {
    super();
    this.ID = ID;
    this.ID = ID;
  }

  public ID(ID ID) {
    this(ID.ID, ID.ID);
  }

  public ID ID() {
    return ID;
  }

  public void ID(ID ID) {
    this.ID = ID;
  }

  public ID ID() {
    return ID;
  }

  public void ID(ID ID) {
    this.ID = ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public void ID(ID<ID> ID) {
    this.ID = ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public void ID(ID<ID> ID) {
    this.ID = ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public void ID(ID<ID> ID) {
    this.ID = ID;
  }

  public boolean ID() {
    return ID;
  }

  public void ID(boolean ID) {
    this.ID = ID;
  }

  public boolean ID() {
    return ID;
  }

  public void ID(boolean ID) {
    this.ID = ID;
  }

  public boolean ID() {
    return ID;
  }

  public void ID(boolean ID) {
    this.ID = ID;
  }

  public boolean ID() {
    return ID;
  }

  public void ID(boolean ID) {
    this.ID = ID;
  }

  public boolean ID() {
    return ID;
  }

  public void ID(boolean ID) {
    this.ID = ID;
  }

  public boolean ID() {
    return ID;
  }

  public void ID(boolean ID) {
    this.ID = ID;
  }

  @ID
  public ID ID() {
    ID ID = ID.ID();
    if (ID.ID(STRING_DQ))
      return ID;
    else
      return ID + STRING_DQ + ID;
  }

  @ID
  public int ID() {
    final int ID = INTEGER;
    int ID = this.ID();
    ID = ID * ID + ((ID == null) ? INTEGER : ID.ID());
    ID = ID * ID + ((ID == null) ? INTEGER : ID.ID());
    return ID;
  }

  @ID
  public boolean ID(ID ID) {
    if (this == ID) return true;
    if (ID() != ID.ID()) return false;
    ID ID = (ID) ID;
    if (ID == null) {
      if (ID.ID != null) return false;
    } else if (!ID.ID(ID.ID)) return false;
    if (ID == null) {
      if (ID.ID != null) return false;
    } else if (!ID.ID(ID.ID)) return false;
    return true;
  }

  @ID
  public int ID(ID ID) {
    COMMENT_LINE_C

    int ID = this.ID();
    int ID = ID.ID();

    if (ID > ID) return INTEGER;
    if (ID < ID) return -INTEGER;

    COMMENT_LINE_C
    int ID = this.ID.ID().ID(((ID) ID).ID.ID());
    if (ID != INTEGER) return ID;

    return ID.ID(((ID) ID).ID);
  }
}


public class ID extends ID {
  ID ID;

  @ID
  public ID ID() {
    return ID;
  }

  @ID
  public ID ID() {
    return STRING_DQ + ID();
  }

  @ID
  public ID ID(ID ID) {
    return STRING_DQ + ID + STRING_DQ + ID();
  }
}


public abstract class ID extends ID {

  protected final ID ID;

  public ID ID() {
    return ID;
  }

  public ID(ID ID) {
    super();
    this.ID = ID;
  }

  @ID
  public int ID() {
    final int ID = INTEGER * ID();
    int ID = INTEGER;
    ID = ID * ID + ((ID == null) ? INTEGER : ID.ID());
    return ID;
  }

  @ID
  public boolean ID(ID ID) {
    if (this == ID) return true;
    if (ID == null) return false;
    if (ID() != ID.ID()) return false; COMMENT_LINE_C
    ID ID = (ID) ID;
    if (ID == null) {
      if (ID.ID != null) return false;
    } else if (!ID.ID(ID.ID)) return false;
    return true;
  }

  @ID
  public int ID(ID ID) {
    COMMENT_LINE_C

    int ID = this.ID();
    int ID = ID.ID();

    if (ID > ID) return INTEGER;
    if (ID < ID) return -INTEGER;

    COMMENT_LINE_C
    return ID.ID(((ID) ID).ID);
  }
}





public class ID extends ID {

  public ID(ID ID) throws ID {
    super(ID);
    if (!ID.ID(ID)) throw new ID(STRING_DQ + ID);

  }

  @ID
  public ID ID() {
    return STRING_DQ + ID;
  }

  @ID
  public ID ID() {
    return ID.ID(ID);
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }

}




public class ID extends ID {

  public ID(ID ID) {
    super(ID);
  }

  @ID
  public ID ID() {
    return STRING_DQ + ID + STRING_DQ;
  }

  @ID
  public ID ID() {
    return ID.ID(ID);
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }
}




public class ID extends ID {

  public ID(ID ID) {
    super(ID);
  }

  @ID
  public ID ID() {
    return STRING_DQ + ID + STRING_DQ;
  }

  @ID
  public ID ID() {
    return ID.ID(ID);
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }
}




public class ID extends ID {

  public ID(ID ID) {
    super(ID);
  }

  @ID
  public ID ID() {
    return STRING_DQ + ID;
  }

  @ID
  public ID ID() {
    return ID.ID(ID);
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }
}







public class ID extends ID {
  ID<ID> ID = new ID<>();

  public ID(ID ID, ID ID) throws ID {
    super(null);
    if (ID.ID() > INTEGER) throw new ID(STRING_DQ);
    if (ID.ID() > INTEGER) throw new ID(STRING_DQ);
    char ID = ID.ID(INTEGER), ID = ID.ID(INTEGER);
    for (char ID = ID; ID <= ID; ID++)
      ID.ID(ID);
  }

  @ID
  public ID ID() {
    return STRING_DQ + ID;
  }

  @ID
  public ID ID() {
    return ID.ID(ID.ID());
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }
}




public class ID extends ID {

  public ID(ID ID) {
    super(ID);
    COMMENT_LINE_C
  }

  @ID
  public ID ID() {
    return ID;
  }

  @ID
  public ID ID() {
    return ID.ID(ID);
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }

}




public class ID extends ID {

  public ID(ID ID) {
    super(ID);
  }

  @ID
  public ID ID() {
    return ID.ID(ID);
  }

  @ID
  public ID ID(ID ID) {
    if (ID == null)
      return STRING_DQ + ID();
    else
      return STRING_DQ + ID + STRING_DQ + ID();
  }
}









public class ID extends ID implements ID<ID> {

  protected final ID<ID> ID = new ID<ID>();
  protected final ID<ID> ID = new ID<ID>();
  private final ID<ID> ID = new ID<ID>();

  public ID<ID> ID() {
    return ID;
  }

  public ID<ID> ID() {
    return ID;
  }

  public ID ID = ID.ID; COMMENT_LINE_C
  public int ID; COMMENT_LINE_C
  ID ID; COMMENT_LINE_C
  COMMENT_LINE_C
  COMMENT_LINE_C
  ID ID; COMMENT_LINE_C
  char ID; COMMENT_LINE_C

  ID ID; COMMENT_LINE_C
  ID ID; COMMENT_LINE_C
  ID ID; COMMENT_LINE_C
  int ID = INTEGER; COMMENT_LINE_C
  ID ID; COMMENT_LINE_C
  ID ID;
  ID ID;
  ID ID;
  ID ID; COMMENT_LINE_C
  ID ID; COMMENT_LINE_C
  ID ID; COMMENT_LINE_C

  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C

  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  public boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C

  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C
  boolean ID; COMMENT_LINE_C

  public ID(int ID, ID ID) {
    super(ID, ID);
    COMMENT_LINE_C
  }

  @ID
  public int ID(ID ID) {
    if ((ID) ID == (ID) (ID.ID)) return INTEGER;
    if ((ID) ID < (ID) (ID.ID))
      return -INTEGER;
    else
      return INTEGER;
  }

  @ID
  public ID ID() {
    return (ID) ID;
  }

  @ID
  public ID ID() {
    return (ID) ID;
  }

  @ID
  public ID ID() {
    return (ID) ID;
  }

  @ID
  public ID ID() {
    return (ID) ID;
  }

  public int ID() {
    return ID;
  }

  public ID ID(ID ID) {
    return (ID) super.ID(ID);
  }

  public ID ID() {
    ID ID = STRING_DQ;
    ID ID = ID().ID();
    ID ID = ID.ID(ID.ID(ID) + ID.ID()).ID();

    ID ID = ID + STRING_DQ + STRING_DQ + ID + STRING_DQ + ID + STRING_DQ + ID()
        + (ID == null ? STRING_DQ : (this instanceof ID ? STRING_DQ : STRING_DQ + ID.ID())) + ID
        + (ID == null ? STRING_DQ : STRING_DQ + ID) + (ID == null ? STRING_DQ : STRING_DQ + ID) + STRING_DQ + (ID ? STRING_DQ : STRING_DQ)
        + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ)
        + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ)
        + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ)
        + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ)
        + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ)

        + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ)

        + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ) + (ID ? STRING_DQ : STRING_DQ)
        + (ID ? STRING_DQ : STRING_DQ) + ID(ID, STRING_DQ) + ID(ID(), STRING_DQ)
        + ID(ID(), STRING_DQ) + STRING_DQ + ID + ID(ID, STRING_DQ) + ID(ID, STRING_DQ)
        + ID(ID, STRING_DQ) + ID(ID, STRING_DQ) + ID(ID, STRING_DQ) + ID(ID, STRING_DQ) + ID(ID, STRING_DQ);

    if (!ID.ID()) ID += STRING_DQ + ID;
    if (!ID.ID()) ID += STRING_DQ + ID;
    if (!ID().ID()) ID += STRING_DQ + ID();

    ID += STRING_DQ + (ID == null ? STRING_DQ : ID.ID());
    ID += STRING_DQ + (ID == null ? STRING_DQ : ID.ID());
    return ID;
  }

  COMMENT_LINE_C
  ID ID(ID ID) {
    return ID;
  }

  @ID
  public void ID(ID ID, ID ID) {
    ID.ID(STRING_DQ + ID + STRING_DQ + ID() + STRING_DQ);
    COMMENT_LINE_C
    for (ID ID = (ID) ID; ID != null; ID = (ID) ID.ID) {
      ID.ID(STRING_DQ + ID + STRING_DQ + ID.ID() + STRING_DQ);
      ID.ID(ID, ID);
    }
  }

  public void ID(ID ID, int ID) {
    if (ID != INTEGER) {
      ID.ID(STRING_DQ + ID + STRING_DQ);
      ID.ID(ID + STRING_DQ + ID + STRING_DQ + ID() + STRING_DQ);
    }
    COMMENT_LINE_C
    for (ID ID = (ID) ID; ID != null; ID = (ID) ID.ID)
      ID.ID(ID, ID + INTEGER);
    if (ID != INTEGER) ID.ID(ID + STRING_DQ + STRING_DQ);
  }

  public ID ID() {
    if (ID() == null) return STRING_DQ;

    return ((ID) ID().ID()).ID().ID() + STRING_DQ + ((ID) ID().ID()).ID()
        + (ID ? STRING_DQ : STRING_DQ + ID());
  }

  public ID ID(ID ID) {
    return STRING_DQ + ID + STRING_DQ + ID();
  }

  public ID ID(ID ID, ID ID) {
    if (ID == null || ID == this)
      return STRING_DQ;
    else
      return STRING_DQ + ID + STRING_DQ + ID.ID();
  }

  ID ID() {
    return this.ID() == null ? STRING_DQ : STRING_DQ;
  }

  boolean ID = false;

  protected ID ID(ID ID, ID ID, ID ID, boolean ID) {

    COMMENT_LINE_C
    if (this instanceof ID) ID += STRING_DQ + ID();

    if (this instanceof ID) ID += ID().ID() + STRING_DQ + ID();

    if (this instanceof ID) ID += ID().ID() + STRING_DQ + ID();

    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();

    if (this instanceof ID) if (this == ID) {
      ID += ID + STRING_DQ + ID();
      if (ID) {
        ID = true;
        return ID;
      }
    }

    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();

    if (ID) return ID;

    COMMENT_LINE_C
    for (ID ID = ID(); ID != null; ID = ID.ID()) {
      ID = ID.ID(ID, ID, ID, ID);

      if (ID.ID() != null) {
        if (this instanceof ID) ID += STRING_DQ + ID();
        if (this instanceof ID) ID += STRING_DQ + ID();
        if (this instanceof ID) ID += STRING_DQ + ID();
        if (this instanceof ID) ID += STRING_DQ + ID();
      }
    }

    COMMENT_LINE_C
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();
    if (this instanceof ID) ID += STRING_DQ + ID();

    return ID;
  }

  private boolean ID(ID ID) {
    boolean ID = false;

    if (this == ID) return true;

    for (ID ID = ID(); ID != null; ID = ID.ID())
      ID |= ID.ID(ID);

    return ID;
  }

  public ID ID() {
    return ID(STRING_DQ);
  }

  public ID ID(ID ID) {
    if (this instanceof ID) return STRING_DQ + ID.ID();

    ID = false;
    ID ID = ID();
    ID ID = ID();
    ID ID = STRING_DQ;
    if (ID != null && ID.ID() != null) ID = ID.ID().ID();
    if (ID != null)
      return ID.ID(this, ID, ID + STRING_DQ, false);
    else
      return STRING_DQ + ID + STRING_DQ + ID + STRING_DQ;
  }

  public ID ID() {
    return ID + ID;
  }

  public ID ID() {
    if (ID != null)
      return ID;
    else
      return ID();
  }

  public ID ID() { COMMENT_LINE_C
    ID ID = STRING_DQ;
    for (ID ID = ID().ID(); ID != this; ID = ID.ID())
      if (!(ID instanceof ID || ID instanceof ID)) ID += ID.ID + STRING_DQ;

    return ID;
  }

  public ID ID() {
    return ID;
  }

  public ID ID() {
    return ID;
  }

  public ID ID() {
    if (this instanceof ID) return STRING_DQ;
    if (ID != null) return ID.ID();
    return STRING_DQ;
  }

  public ID<ID> ID() {
    return ID;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }

}


public class ID extends ID {
  ID ID;

  public ID(int ID, ID ID) {
    super(ID, new ID(ID));
    this.ID = ID;
  }

  @ID
  public ID ID() {
    return ID;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID, ID ID) {
    super(ID, ID);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID, ID ID) {
    super(ID, null);
    ID.ID(ID);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID, ID ID) {
    super(ID, ID);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }

}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID, ID ID) {
    super(ID, ID);
    ID = true;
    ID = this;
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ + ID;
  }
}


public class ID extends ID {

  public ID(int ID, ID ID) {
    super(ID, ID);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ + ID;
  }

  @ID
  public ID ID() {
    return ((ID) ID).ID() + ID;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }
}


public class ID extends ID {

  public ID(int ID) {
    super(ID, null);
  }

  @ID
  public ID ID() {
    return COMMENT_BLOCK_C ID(STRING_DQ);
  }

}


public class ID extends ID {

  public ID(int ID, ID ID) {
    super(ID, ID);
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ;
  }

}


public class ID extends ID {

  public ID(int ID, ID ID) {
    super(ID, ID);
    ID.ID(ID); COMMENT_LINE_C
  }

  @ID
  public ID ID() {
    return ID + STRING_DQ + ID;
  }
}

